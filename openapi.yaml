openapi: 3.1.0
info:
  title: SpecR API
  version: 0.1.0
  license:
    name: MIT
    identifier: MIT
  description: >
    Headless REST API for CSI® MasterFormat®-compatible specification document
    automation with round-trip DOCX support. Independent project; not affiliated
    with CSI.

# No authentication yet — auth lands with Phase 5f (#43). The explicit empty
# security array documents that every endpoint is currently unauthenticated.
security: []

servers:
  - url: http://localhost:3000
    description: Local development server

# ─── Paths ────────────────────────────────────────────────────────────────────

paths:
  /health:
    get:
      operationId: getHealth
      summary: Liveness check
      tags: [system]
      responses:
        '200':
          description: Server is alive and the database is reachable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [db, uptime]
                        properties:
                          db:
                            type: string
                            description: Database connectivity state ("connected")
                          uptime:
                            type: integer
                            description: Process uptime in whole seconds
        '503':
          description: 'Database unavailable — `{success: false, error: "database unavailable"}`'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /parse:
    post:
      operationId: parseDocument
      summary: Upload and parse a spec document (async)
      description: >
        Upload a `.docx`, `.sec`, `.txt`, or `.pdf` file. Returns 202 immediately with a
        `jobId`; poll `/parse/jobs/{jobId}` for progress and result. Rate-limited per IP
        to a configurable quota (default 10 requests/minute via `RATE_LIMIT_UPLOAD_MAX`;
        disable with `DISABLE_RATE_LIMIT`). Optionally pass `numberingProfileId` to apply
        a saved structural numbering profile (#299) as a deterministic override during
        this parse; omit it for the default 5-signal inference. Profile application is at
        parse time — an already-parsed spec is not re-applied (SpecR keeps no source DOCX).
      tags: [parse]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Spec document (.docx, .sec, .txt, or .pdf). Max 10 MB.
                section:
                  type: string
                  description: Override the detected CSI section number (e.g. "09 91 26").
                title:
                  type: string
                  description: Override the detected section title.
                numberingProfileId:
                  type: string
                  format: uuid
                  description: >
                    Optional saved numbering profile to apply as a deterministic
                    structural override (#299). Must exist (→ 404 if not). Applies to
                    DOCX inference; omit for default 5-signal behavior.
      responses:
        '202':
          description: Parse job accepted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [jobId]
                        properties:
                          jobId:
                            type: string
                            format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /parse/jobs/{jobId}:
    get:
      operationId: getParseJob
      summary: Poll parse job progress
      tags: [parse]
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Job status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ParseJob'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /search:
    get:
      operationId: searchParagraphs
      summary: Ranked full-text paragraph search
      description: >
        Ranked full-text search over paragraph content (ADR-062): PostgreSQL
        `websearch_to_tsquery` + `ts_rank_cd` (proximity-aware, so a tight cluster
        of the query terms outranks the same terms scattered across a long
        paragraph) with `ts_headline` snippets. A degenerate (no-lexeme) query
        falls back to a substring scan. Every hit carries stable anchors — spec id,
        section, title, and paragraph UUID — so a client can navigate straight to
        the result. Scope with `libraryId`/`projectId` (a spec is a library master
        or a project working copy), `division`, `part` (CSI PART 1/2/3), and
        `nodeType`. Hidden (`meta.vanish`) content is not filtered — retrieval
        surfaces all stored text; suppression is a render concern.
      tags: [search]
      parameters:
        - name: q
          in: query
          required: true
          description: Search text (natural language or keywords; supports quoted phrases, `or`, `-negation`).
          schema:
            type: string
            minLength: 1
        - name: libraryId
          in: query
          required: false
          description: Scope to specs owned by this library master.
          schema:
            type: string
            format: uuid
        - name: projectId
          in: query
          required: false
          description: Scope to specs owned by this project.
          schema:
            type: string
            format: uuid
        - name: division
          in: query
          required: false
          description: 2-digit CSI division, e.g. "27".
          schema:
            type: string
            pattern: '^\d{2}$'
        - name: part
          in: query
          required: false
          description: CSI PART ordinal — 1 (General), 2 (Products), or 3 (Execution).
          schema:
            type: integer
            minimum: 1
            maximum: 3
        - name: nodeType
          in: query
          required: false
          description: >-
            Restrict to a paragraph node type (part, article, pr1…pr7, note,
            or the object/objectText body-object types, #300).
          schema:
            type: string
            enum:
              [
                spec,
                part,
                article,
                pr1,
                pr2,
                pr3,
                pr4,
                pr5,
                pr6,
                pr7,
                note,
                continuation,
                object,
                objectText,
              ]
        - name: limit
          in: query
          required: false
          description: Maximum results (1–100, default 20).
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: Ranked paragraph hits (highest rank first)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        maxItems: 100
                        items:
                          $ref: '#/components/schemas/SearchHit'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}:
    get:
      operationId: getSpec
      summary: Retrieve a spec with its full paragraph tree
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Spec with paragraph tree
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/SpecTree'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    patch:
      operationId: updateSpec
      summary: Update spec metadata
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                section:
                  type: string
                  description: CSI section number (e.g. "09 91 26").
                title:
                  type: string
      responses:
        '200':
          description: Updated spec summary
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/SpecSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: withdrawSpec
      summary: Soft-withdraw (tombstone) a library master
      description: >
        Soft-withdraws a library master (ADR-030): tombstones it with
        `withdrawn_at`. The row, its paragraphs, and `parent_spec_id` lineage
        edges stay intact — the master is hidden from listings/resolution
        (`GET /libraries/{id}/specs`, project source resolution, broken-ref
        `availableFrom`, the coordination present set) but remains retrievable
        by id (with `withdrawnAt` surfaced) and reversible via
        `POST /specs/{id}/restore` — and a re-import of the same section/source
        into the library also revives it (#415), replacing its content. Idempotent —
        a re-withdraw returns the EXISTING `withdrawnAt`, never a new
        timestamp. Withdrawal targets library masters only; on a project copy
        it returns 409.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Master withdrawn (or already was)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [specId, withdrawnAt]
                        properties:
                          specId:
                            type: string
                            format: uuid
                          withdrawnAt:
                            type: string
                            format: date-time
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >
            The id is a project copy, not a library master — withdrawal applies
            to masters. Remove a project copy via
            `DELETE /projects/{id}/specs/{specId}` instead.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/restore:
    post:
      operationId: restoreSpec
      summary: Restore a withdrawn library master
      description: >
        Clears a library master's withdrawal tombstone (ADR-030), making it
        visible in listings/resolution again. Idempotent — restoring a master
        that is not withdrawn is a 200 no-op. On a project copy it returns 409
        (withdrawal/restore applies to masters).
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Master restored (or was already active)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [specId]
                        properties:
                          specId:
                            type: string
                            format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >
            The id is a project copy, not a library master — withdrawal/restore
            applies to masters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/paragraphs:
    post:
      operationId: insertParagraph
      summary: Insert a new paragraph after an anchor node
      description: >
        Inserts a new paragraph immediately after `anchorNodeId`, as its
        sibling (same parent), shifting the following siblings' positions
        (#372). CSI labels are render-derived from node type + sibling
        ordinal, so consumers renumber automatically. `nodeType` defaults to
        the anchor's own type; only `article`, `pr1`–`pr7`, and `continuation`
        are insertable — an anchor whose type falls outside that set (a part
        or note) requires an explicit `nodeType`, otherwise the request is
        rejected 422. Parts are never insertable (a CSI section keeps its
        three-part shape) and notes materialize via accept-as-note instead.


        Inserting is a content write: it passes the composed edit gate
        (ADR-018) and bumps the spec's `contentVersion`. Supply
        `expectedVersion` to make the write conditional — a stale value is
        rejected 409 with the current version. The anchor must belong to the
        spec in the path; otherwise the request is rejected with 403.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [anchorNodeId, text]
              properties:
                anchorNodeId:
                  type: string
                  format: uuid
                  description: >
                    The paragraph the new node is inserted after (same
                    parent, next position).
                text:
                  type: string
                  minLength: 1
                  description: New paragraph text (must be non-empty).
                nodeType:
                  type: string
                  enum: [article, pr1, pr2, pr3, pr4, pr5, pr6, pr7, continuation]
                  description: >
                    Node type for the new paragraph. Defaults to the anchor's
                    own type.
                expectedVersion:
                  type: integer
                  minimum: 1
                  description: >
                    Optional optimistic-concurrency precondition — the spec
                    `contentVersion` the caller read. Stale → 409.
                actorLabel:
                  $ref: '#/components/schemas/ActorLabel'
      responses:
        '201':
          description: Created paragraph node
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/SpecNode'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WriteConflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /specs/{id}/paragraphs/{nodeId}:
    patch:
      operationId: updateParagraph
      summary: Update a single paragraph's text by UUID
      description: >
        Replaces one paragraph's text in place (ADR-009), bumping its
        `base_version` and the spec's `contentVersion`. The Revit add-in pushes
        model-parameter changes here without replacing the whole spec. The node
        must belong to the spec in the path; otherwise the request is rejected
        with 403.


        Optimistic concurrency (ADR-018): supply `expectedVersion` (the spec
        `contentVersion` you read) to make the write conditional — a stale value
        is rejected 409 with the current version, so a concurrent edit is never
        silently clobbered. The composed edit gate also rejects writes to an
        archived spec or one locked upstream in a DMS (409).


        A body `object` node (#300, ADR-072) is locked — its content is a
        captured OOXML blob, editable only through its `objectText` children —
        so a direct write to it is rejected 422.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [text]
              properties:
                text:
                  type: string
                  minLength: 1
                  description: New paragraph text (must be non-empty).
                expectedVersion:
                  type: integer
                  minimum: 1
                  description: >
                    Optional optimistic-concurrency precondition — the spec
                    `contentVersion` the caller read. Stale → 409.
                actorLabel:
                  $ref: '#/components/schemas/ActorLabel'
      responses:
        '200':
          description: Updated paragraph node (with its subtree)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/SpecNode'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WriteConflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/paragraphs/{nodeId}/editability:
    patch:
      operationId: patchEditability
      summary: Set or clear a paragraph's human editability override
      description: >
        Records the reviewer's correction as the paragraph's `editability_override`
        (ADR-022 D2), stored beside — never merged into — the machine classification.
        `editability: null` clears the override so the effective value falls back to
        the machine verdict. The node must belong to the spec in the path (else 403).
        Setting a non-null override on an `object` or `objectText` node is rejected
        422 — their editability is fixed at capture time (ADR-072 decision 2: an
        `object` always `locked`, its `objectText` children always `editable`) and
        is never a human-editable choice; clearing (`null`) such a node is a no-op
        ownership-checked 200, since there is never a standing override to clear.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [editability]
              properties:
                editability:
                  oneOf:
                    - type: string
                      enum: [locked, editable, choice, note]
                    - type: 'null'
                  description: Override value, or null to clear it.
      responses:
        '200':
          description: Override applied (or cleared)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [nodeId, editability]
                        properties:
                          nodeId:
                            type: string
                            format: uuid
                          editability:
                            oneOf:
                              - type: string
                                enum: [locked, editable, choice, note]
                              - type: 'null'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/paragraphs/{nodeId}/removal:
    patch:
      operationId: removeParagraph
      summary: Remove or restore a paragraph (reversible soft removal)
      description: >
        The editability program's reversible paragraph removal (#251, ADR-022).
        `removed: true` sets the node's `vanish` flag — it is suppressed from the
        owner-facing renders (DOCX and Markdown) while the row, its subtree, and
        any contained cross-references stay intact; `removed: false` reverses it.
        This is a soft, reversible removal by design (symmetric with the spec
        soft-delete in ADR-030), not a hard cascading DELETE. Only body paragraphs
        are removable: a structural heading (`part`/`article`) or a `note` node —
        which the owner-facing renderers cannot suppress — is rejected 422. The
        toggle is idempotent: re-sending the same `removed` value is a no-op that
        returns the node unchanged without minting a new content version. The node
        must belong to the spec in the path (else 403). The composed edit gate
        (ADR-018) rejects an archived spec or one locked upstream in a DMS (409).
        Every model-driven render honors removal: the `.SEC` egress filters an
        owner-removed body paragraph (and its subtree), consistent with DOCX and
        Markdown (#278, ADR-060), so removed content never appears in an export.
        This is a filter, not a lossless encode — SEC's hidden marker already means
        "specifier note", so removal is not round-tripped back as `vanish` — and it
        is latent regardless, as there is no `.SEC` export endpoint today.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [removed]
              properties:
                removed:
                  type: boolean
                  description: >
                    true to remove (set vanish), false to restore (clear vanish).
                actorLabel:
                  $ref: '#/components/schemas/ActorLabel'
      responses:
        '200':
          description: Updated paragraph node (with its subtree)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/SpecNode'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WriteConflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/reclassify:
    post:
      operationId: reclassifySpec
      summary: Re-run editability classification over stored source facts
      description: >
        Re-classifies every paragraph from its persisted `source_facts` (ADR-022 D4)
        with no source document — the architectural point of the substrate. Supply
        `rules` to preview candidate conventions; omit them to resolve the spec's
        library convention profile (built-in default fallback). `preview: true`
        computes the diff without persisting. Standing human overrides are never
        touched; the report flags where the fresh machine verdict now disagrees with
        an override (ADR-022 D2).
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                rules:
                  $ref: '#/components/schemas/ConventionRules'
                preview:
                  type: boolean
                  description: Compute the diff without persisting (preview-before-save).
      responses:
        '200':
          description: Before/after classification diff report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ReclassifyReport'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/finalize:
    post:
      operationId: finalizeSpec
      summary: Finalize onboarding (review → active)
      description: >
        Marks a human as having reviewed the machine's first pass: flips
        `onboardingStatus` from `review` to `active` (ADR-022 D6). Also snapshots
        the spec's resolved convention rules into its library's OWN convention
        profile, so the next import from that library self-classifies against them
        instead of falling back to the built-in default. `active` does NOT seal the
        spec — corrections (#136), reclassify, conventions (#137) and style-source
        (#138) endpoints keep working. Idempotent: finalizing an already-active spec
        returns 200 unchanged.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Onboarding finalized (or already active)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [onboardingStatus]
                        properties:
                          onboardingStatus:
                            type: string
                            enum: [active]
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/reopen:
    post:
      operationId: reopenSpec
      summary: Reopen onboarding (active → review)
      description: >
        Flips `onboardingStatus` from `active` back to `review` (ADR-022 D6).
        Purely informational — no endpoint behavior gates on it. Reopening an
        already-`review` spec returns 200 unchanged.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Onboarding reopened (or already in review)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [onboardingStatus]
                        properties:
                          onboardingStatus:
                            type: string
                            enum: [review]
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/paragraphs/{nodeId}/comments/{index}/accept-as-note:
    post:
      operationId: acceptCommentAsNote
      summary: Materialize a margin comment as a note node
      description: >
        Inserts the margin comment at `index` (captured in `source_facts.comments`)
        as a `note` paragraph immediately after the anchor (ADR-022 D4 — never a
        silent tree mutation). Repeating the call returns 409 with the existing
        note's id (idempotent contract). The node must belong to the spec (else 403).


        Materializing a note is a content write: it passes the composed edit gate
        (ADR-018) and bumps the spec's `contentVersion`. Accepting against an
        archived or upstream-locked spec is rejected with 409.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
        - name: index
          in: path
          required: true
          schema:
            type: integer
            minimum: 0
          description: Zero-based index into the anchor's source_facts.comments.
      requestBody:
        required: false
        description: >
          Optional (#377) — the route was bodyless pre-#377 and a truly absent
          body still resolves as if `{}` were sent (byte-identical behavior).
        content:
          application/json:
            schema:
              type: object
              properties:
                actorLabel:
                  $ref: '#/components/schemas/ActorLabel'
      responses:
        '201':
          description: Note node created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [noteId]
                        properties:
                          noteId:
                            type: string
                            format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: >
            Either the comment was already accepted (the existing note's id
            travels back as `noteId`), or the spec is archived / upstream-locked
            and rejected by the edit gate (no `noteId`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ErrorResponse'
                  - type: object
                    properties:
                      noteId:
                        type: string
                        format: uuid
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/paragraphs/{nodeId}/associations:
    get:
      operationId: listAssociations
      summary: List external content associations for a paragraph
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
      responses:
        '200':
          description: List of paragraph associations
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ParagraphAssociation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createAssociation
      summary: Associate external content with a paragraph
      description: >
        Links a DMS document or direct URL to a paragraph. Either a complete DMS
        pair (externalProvider + externalId) or a url must be provided.
        Only link + provenance is stored — never the licensed bytes (ADR-019).
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAssociation'
      responses:
        '201':
          description: Association created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ParagraphAssociation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/paragraphs/{nodeId}/associations/{associationId}:
    delete:
      operationId: deleteAssociation
      summary: Remove an external content association from a paragraph
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
        - $ref: '#/components/parameters/NodeId'
        - $ref: '#/components/parameters/AssociationId'
      responses:
        '204':
          description: Association deleted
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/lineage:
    get:
      operationId: getSpecLineage
      summary: Chain of custody for a spec
      description: >
        Walks `parent_spec_id` from the requested spec to the root library master,
        returning every hop in leaf-first order (`chain[0]` = the requested spec,
        last entry = the root).


        `behindBy` for each hop = the parent's current `contentVersion` minus this
        copy's `originVersion` (the version it was cloned from); null on the root
        because the root has no parent to drift behind.


        `originMeta` is the root's ingest provenance (`filename`, `sha256`, `loader`)
        when the root was file-ingested; null otherwise.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Custody chain from requested spec to root
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/SpecLineage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/hierarchy-report:
    get:
      operationId: getSpecHierarchyReport
      summary: Per-paragraph hierarchy-inference scoring report
      description: >
        Every scored structural paragraph in the spec, worst confidence first,
        labeled exactly as a client would see it in the rendered markdown
        (WS2, #424). `counts` and `unscoredReason` mirror the `hierarchy` field
        of `OnboardingReport` (ADR-055) exactly; `paragraphs` carries ALL scored
        nodes (not just the below-threshold subset) so a caller can review the
        full confidence distribution, not only what already tripped the review
        threshold.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Hierarchy-inference scoring report, worst-first
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/HierarchyReport'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/style-source:
    post:
      operationId: setSpecStyleSource
      summary: Assign a style template to a spec (manual style-source pick)
      description: >
        Associates an existing style template (#30/#31) with the spec so the
        generator can render it with a real house style (#138). One template per
        spec; re-assigning replaces the previous template. `.SEC`/`.txt`/PDF
        masters carry no adopted visual style, so this is the narrowest useful
        slice of #125's style sources. Both the spec and the template must
        already exist — either missing → 404 (pre-checked, never via the FK error).
        A template concurrently deleted during assignment is also reported as 404,
        not a scope conflict (#366). A template assigned to any spec cannot be
        deleted (DELETE /templates/{id} → 409). Library scoping (#318): the template
        must be a built-in / global template (`libraryId: null`) or owned by the
        spec's own library; a template from another library is rejected with 409.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [templateId]
              properties:
                templateId:
                  type: string
                  format: uuid
                  description: Style template to associate with the spec.
      responses:
        '200':
          description: Style source assigned
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/StyleSource'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: clearSpecStyleSource
      summary: Clear a spec's style template (fall back to generator defaults)
      description: >
        Removes the spec→template association (#138). Idempotent — clearing an
        already-unset association is still 200. The spec must exist (unknown → 404).
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Style source cleared (styleSource is null)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [styleSource]
                        properties:
                          styleSource:
                            type: 'null'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/generate:
    post:
      operationId: generateDocx
      summary: Generate a DOCX from a stored spec AST
      description: >
        Header/footer content is resolved automatically (#304): when the spec
        belongs to exactly one project, and that project's client → project →
        package → revision scope chain has at least one configured
        `header_footer_configs` layer, the resolved composition and field
        values (project name, client name, etc.) are rendered into the
        generated DOCX, stamped with the current date. An orphan spec, a spec
        owned by two or more projects, or a resolvable project with no
        configured layers anywhere in its chain all fall back gracefully — the
        DOCX renders with no header/footer, identical to the pre-#304 output.
        This resolution has no request or response shape of its own; it is
        driven entirely by existing project/package/revision and
        `header_footer_configs` state.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                templateId:
                  type: string
                  format: uuid
                  description: >
                    Style template to apply (font, spacing, indent, numbering format).
                    Omitted → the seeded UFGS-Default template is applied; if that template
                    is absent (un-migrated database) the response is still 200 with an
                    unstyled DOCX. Unknown id → 404.
                sectionNumberFormat:
                  type: string
                  enum: [canonical, dots, compact, spaced-compact]
                  description: >
                    Presentation format for generated section numbers in the synthetic
                    DOCX title and confidently identified Section references in paragraph
                    text. Canonical emits "09 91 00", dots emits "09.91.00", compact
                    emits "099100", and spaced-compact emits "09 9100". Stored AST and
                    database values remain canonical. Omitted → the sole owning project's
                    override, then its client's firm default, then canonical. If the spec
                    belongs to no project (or several), canonical applies.
            examples:
              dotted-section-numbers:
                value:
                  sectionNumberFormat: dots
      responses:
        '200':
          description: DOCX file stream
          content:
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/diff:
    post:
      operationId: diffSpecDocx
      summary: Diff a returned DOCX against stored spec state
      description: >
        Upload an edited DOCX generated by SpecR. The response is a UUID-keyed
        3-way diff: base snapshot, current DB state (`ours`), and returned DOCX
        content (`theirs`). The synthetic title paragraph is ignored because it
        is not a persisted AST node.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Returned SpecR-generated DOCX. Max 10 MB.
      responses:
        '200':
          description: Structured merge diff
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/DiffResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/merge:
    post:
      operationId: mergeSpecDiff
      summary: Apply accepted changes from a spec diff
      description: >
        Submit accepted change UUIDs along with the `DiffResult` previously
        returned by `/specs/{id}/diff`. Accepted modified/conflict entries update
        `paragraphs.text`, insert a `paragraph_versions` snapshot for the new
        base version, and increment `paragraphs.base_version`. Rejected entries
        are discarded by omission. A successful merge bumps the spec's
        `contentVersion` at most once — not once per applied entry — and only
        when at least one change is actually applied; a no-op merge (nothing
        accepted) leaves `contentVersion` unchanged.


        Accepted `diff.added` entries insert the new paragraph immediately
        after `afterUuid` (the nearest preceding controlled paragraph in
        document order) — the same sibling-insert primitive `POST
        /specs/{id}/paragraphs` uses. An addition with no `afterUuid` (nothing
        precedes it, including table-cell paragraphs) cannot currently be
        applied and is rejected 400; accept the redlined DOCX to capture it by
        hand instead. An addition whose `afterUuid` resolves to a structural
        node (part/article/note, outside the pr1–pr7/continuation body tier) is
        also rejected 400 — an orphan carries no tier to become that node's
        sibling. Re-submitting an already-applied addition is a no-op, not a
        duplicate (a re-used uuid naming a different-text or different-spec row
        is rejected 400, not silently duplicated). Accepted `diff.deleted`
        entries reversibly `vanish` the paragraph (ADR-022) —
        the same as `PATCH .../removal` — snapshotting its prior text into
        `paragraph_versions`; only body paragraphs are removable this way, a
        `part`/`article`/`note` uuid in `diff.deleted` is rejected 400.


        Optimistic concurrency (ADR-018): supply `expectedVersion` to make the
        merge conditional on the spec being at that `contentVersion` — stale →
        409 with the current version. The composed edit gate also rejects a merge
        on an archived spec or one locked upstream (409).


        `diff.objectConflicts` (#520) entries are detection-only: accepting an
        entry's `objectId` or any of its `affectedUuids` is always rejected
        400 — a structural change on a table/text box (a row/column added or
        removed, a kind change) cannot be auto-merged and must be resolved by
        hand.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MergeRequest'
      responses:
        '200':
          description: Merge apply summary
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/MergeResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/WriteConflict'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/lock:
    get:
      operationId: getSpecLock
      summary: Current advisory lock on a spec
      description: >
        Returns the live advisory lock (ADR-018 D2) or `{ locked: false }` when
        none is held. An expired lock reads as free. Advisory only — a lock never
        blocks a write on its own; the edit gate governs writes.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Lock state
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [locked, lock]
                        properties:
                          locked:
                            type: boolean
                          lock:
                            description: >
                              The live lock when held, or null when the spec is
                              unlocked (or its lock has expired).
                            oneOf:
                              - $ref: '#/components/schemas/LockState'
                              - type: 'null'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: acquireSpecLock
      summary: Acquire (or refresh / steal-after-expiry) an advisory lock
      description: >
        Acquires the soft lock for `holder`. Succeeds when the spec is free, the
        caller already holds it (refresh), or the existing lock has expired
        (steal). A live lock held by another caller is refused with 409 and the
        blocking holder. There is no unlock ceremony — abandoned locks expire.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [holder]
              properties:
                holder:
                  type: string
                  minLength: 1
                  description: Caller identity label (authenticated identity once #43 lands).
                ttlSeconds:
                  type: integer
                  minimum: 1
                  maximum: 3600
                  description: Lock lifetime in seconds (default 900 / 15 min).
      responses:
        '200':
          description: Lock acquired
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/LockState'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: A live lock is held by another caller
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ErrorResponse'
                  - type: object
                    properties:
                      holder:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: releaseSpecLock
      summary: Release an advisory lock
      description: >
        Releases the lock only if `holder` matches the current holder. A release
        by a non-holder (or when no lock is held) is refused with 409.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [holder]
              properties:
                holder:
                  type: string
                  minLength: 1
      responses:
        '200':
          description: Lock released
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /clients:
    get:
      operationId: listClients
      summary: List all clients
      description: Returns every client with its firm-tier section-number default, ordered by name.
      tags: [clients]
      responses:
        '200':
          description: All clients
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ClientSummary'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createClient
      summary: Create a client
      description: >
        Creates a first-class client (ADR-054): an organizational entity that groups
        projects. `name` is unique. An optional `libraryId` links the client to its
        client-tier master library (a link, not a merge) — an unknown `libraryId` is
        rejected 422.
      tags: [clients]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
                libraryId:
                  type: string
                  format: uuid
                  description: Optional link to the client's client-tier master library
                sectionNumberFormat:
                  type: string
                  enum: [canonical, dots, compact, spaced-compact]
                  default: canonical
                  description: Firm-tier default used when an associated project has no override
      responses:
        '201':
          description: Client created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ClientSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /clients/{id}:
    get:
      operationId: getClient
      summary: Retrieve a client with its projects
      description: >
        Returns the client plus its associated active projects (each a full
        ProjectSummary carrying its own `clientId`/`clientName`). Soft-deleted
        projects are excluded, matching `GET /projects`.
      tags: [clients]
      parameters:
        - $ref: '#/components/parameters/ClientId'
      responses:
        '200':
          description: Client with its associated projects
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ClientDetail'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    patch:
      operationId: updateClient
      summary: Update a client's firm-tier section-number default
      tags: [clients]
      parameters:
        - $ref: '#/components/parameters/ClientId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sectionNumberFormat]
              properties:
                sectionNumberFormat:
                  type: string
                  enum: [canonical, dots, compact, spaced-compact]
      responses:
        '200':
          description: Client updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ClientSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /users:
    get:
      operationId: listUsers
      summary: List all users
      description: Returns every user as a summary (id, label, createdAt), ordered by label.
      tags: [users]
      responses:
        '200':
          description: All users
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/UserSummary'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: resolveUser
      summary: Resolve or create a user by label
      description: >
        Idempotently resolves a user by exact-match `label` (ADR-052 D6), creating
        one if none exists. Always 200 — a repeat call with the same label returns
        the same `id`. `label` is case-sensitive and not yet authenticated identity
        (spoofable pre-#43); it carries the same trust tier as the ADR-018 lock
        holder today.
      tags: [users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [label]
              properties:
                label:
                  type: string
                  minLength: 1
                  maxLength: 200
                  pattern: '[\s\S]*\S[\s\S]*'
                  description: >
                    Leading/trailing whitespace is trimmed before resolution, and
                    whitespace-only values are rejected; the 1-200 length bounds apply
                    to the trimmed value.
      responses:
        '200':
          description: User resolved (existing or newly created)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /users/{id}:
    get:
      operationId: getUser
      summary: Retrieve a user
      description: Returns a single user by id.
      tags: [users]
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: The user
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects:
    get:
      operationId: listProjects
      summary: List all projects
      description: Returns every project as an id/name summary, ordered by name.
      tags: [projects]
      responses:
        '200':
          description: All projects
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectListItem'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createProject
      summary: Create a project
      tags: [projects]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, sourceLibraryIds]
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  minLength: 1
                sourceLibraryIds:
                  type: array
                  minItems: 1
                  uniqueItems: true
                  items:
                    type: string
                    format: uuid
                  description: >
                    Ordered source list — priority = array order (1-based). Each must be a
                    company- or client-tier library (ADR-015 D3); no duplicates. Section
                    resolution for POST /projects/{id}/specs walks this list.
      responses:
        '201':
          description: Project created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          description: 'Validation failed, unknown library, or reference-tier source'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}:
    get:
      operationId: getProject
      summary: Retrieve a project with its TOC
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project with TOC
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectWithToc'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    patch:
      operationId: patchProject
      summary: Update project settings
      description: >
        Updates a project's mutable settings. At least one of `name`,
        `sectionNumberFormat`, or `clientId` must be provided. The name must be
        non-empty. There is no uniqueness constraint on project names, so there
        is no 409 path.
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              anyOf:
                - required: [name]
                - required: [sectionNumberFormat]
                - required: [clientId]
              properties:
                name:
                  type: string
                  minLength: 1
                sectionNumberFormat:
                  type: [string, 'null']
                  enum: [canonical, dots, compact, spaced-compact, null]
                  description: >
                    Project override for generated section numbers. Explicit null
                    clears it so the associated client's firm default applies.
                clientId:
                  type: [string, 'null']
                  format: uuid
                  description: >
                    Associate the project with a client (ADR-054). Explicit null
                    disassociates; an unknown client is rejected 422.
      responses:
        '200':
          description: Updated project settings
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId, name, sectionNumberFormat, clientId]
                        properties:
                          projectId:
                            type: string
                            format: uuid
                          name:
                            type: string
                          sectionNumberFormat:
                            type: string
                            enum: [canonical, dots, compact, spaced-compact]
                          clientId:
                            type: [string, 'null']
                            format: uuid
                            description: Associated client (ADR-054), or null when unassociated
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: deleteProject
      summary: Soft-delete (withdraw) a project
      description: >
        Soft-deletes a project (ADR-031): tombstones it with `deleted_at` + the
        caller-supplied `deletedBy` actor. The project is hidden from
        `GET /projects` but remains retrievable by id (with the tombstone
        surfaced) and reversible via `POST /projects/{id}/restore`. Idempotent —
        re-deleting an already-deleted project returns its EXISTING
        `deletedAt`/`deletedBy`, never overwriting them.
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [deletedBy]
              properties:
                deletedBy:
                  type: string
                  minLength: 1
                  description: >
                    Free-text actor recorded in the audit tombstone — there is no
                    user model yet (#43). When auth lands this is the session user.
      responses:
        '200':
          description: Project soft-deleted (or already was)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId, deletedAt, deletedBy]
                        properties:
                          projectId:
                            type: string
                            format: uuid
                          deletedAt:
                            type: string
                            format: date-time
                          deletedBy:
                            type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/restore:
    post:
      operationId: restoreProject
      summary: Restore a soft-deleted project
      description: >
        Clears a project's soft-delete tombstone (ADR-031), making it active and
        visible in `GET /projects` again. Idempotent — restoring a project that
        is not deleted is a 200 no-op.
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project restored (or was already active)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId]
                        properties:
                          projectId:
                            type: string
                            format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/sources:
    put:
      operationId: setProjectSources
      summary: Replace a project's source libraries
      description: >
        Replaces the project's ordered source-library list (priority = array
        order). Each must be a company- or client-tier library (ADR-015 D3); no
        duplicates. Re-ordering does not re-resolve already-derived specs.
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          description: Project UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sourceLibraryIds]
              properties:
                sourceLibraryIds:
                  type: array
                  minItems: 1
                  uniqueItems: true
                  items:
                    type: string
                    format: uuid
      responses:
        '200':
          description: Updated project sources
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId, sources]
                        properties:
                          projectId:
                            type: string
                            format: uuid
                          sources:
                            type: array
                            items:
                              type: object
                              required: [libraryId, name, tier, priority]
                              properties:
                                libraryId:
                                  type: string
                                  format: uuid
                                name:
                                  type: string
                                tier:
                                  type: string
                                  enum: [company, client]
                                priority:
                                  type: integer
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/generate:
    post:
      operationId: generateManual
      summary: Assemble a project manual DOCX from all TOC sections
      description: >
        Renders every section in the project's TOC (MasterFormat / position order)
        into a single DOCX manual (ADR-017 D1). Each section is its own OOXML
        section with an independent numbering instance, so multilevel numbering
        restarts at PART 1 per section. Every paragraph keeps its `w:sdt` UUID
        anchor for round-trip. The manual opens with a cover page (project name +
        description, style-template applied) and a Word TOC field over the section
        headings — Word computes the TOC entries and pagination on open; SpecR emits
        the field, not page numbers; the cover page itself stays headerless (no
        header/footer reference in its `sectPr`). This route renders the live draft
        from `project_specs`; issued and addendum manuals render from frozen package
        revisions via `POST /revisions/{id}/generate`. Header/footer content is
        resolved and rendered per #481: the project's client → project
        `header_footer_configs` chain is resolved once (this route is project-scoped
        and never consults package- or revision-level layers — use
        `POST /revisions/{id}/generate` for those) and the result is rendered into
        every TOC section's own OOXML section (never just the manual's first page),
        so a `first`/title-page variant applies to each section's own opening page.
        A project with zero configured layers falls back to the pre-#481 output
        unchanged.
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                templateId:
                  type: string
                  format: uuid
                  description: >
                    Style template applied to every section. Omitted → the seeded
                    UFGS-Default template; absent default → unstyled 200. Unknown id → 404.
                sectionNumberFormat:
                  type: string
                  enum: [canonical, dots, compact, spaced-compact]
                  description: >
                    Presentation format for generated section numbers in each
                    section title and confidently identified Section references.
                    Omitted → project override, then client/firm default, then
                    canonical.
      responses:
        '200':
          description: DOCX manual file stream
          content:
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Project has no sections to assemble
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{libraryId}/divisions/{division}/general-spec:
    get:
      operationId: getLibraryDivisionGeneralSpec
      summary: Get division general-spec status for a library
      description: >
        Returns the confirmed division-general spec when one exists. Only an exact
        `NN 00 00` section is assigned automatically. If no exact section exists,
        the response is `status: missing` and fallback matches are returned as
        advisory candidates only.
      tags: [libraries]
      parameters:
        - $ref: '#/components/parameters/LibraryId'
        - $ref: '#/components/parameters/Division'
      responses:
        '200':
          description: Division general-spec status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/DivisionGeneralSpecResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: setLibraryDivisionGeneralSpec
      summary: Manually set a library division general spec
      description: >
        Assigns a same-library spec in the requested division as the division
        general spec, or records that this division has no in-scope general
        requirements spec.
      tags: [libraries]
      parameters:
        - $ref: '#/components/parameters/LibraryId'
        - $ref: '#/components/parameters/Division'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetDivisionGeneralSpecBody'
      responses:
        '200':
          description: Manual division-general decision stored
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/DivisionGeneralSpecResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /conventions:
    get:
      operationId: listConventions
      summary: List built-in editing-convention profiles
      description: >
        Returns the read-only built-in (industry default) convention profiles.
        Built-ins (`libraryId: null`) cannot be written through the API.
      tags: [conventions]
      responses:
        '200':
          description: Built-in convention profiles
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/EditingConvention'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries:
    get:
      operationId: listLibraries
      summary: List all libraries
      description: >
        Returns every library (reference, company, and client tiers), ordered by
        tier then name. The seeded built-in reference and company masters are
        always present.
      tags: [libraries]
      responses:
        '200':
          description: All libraries
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Library'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/clients:
    post:
      operationId: createClientLibrary
      summary: Create a client-tier library
      description: >
        Creates a client library under a company-tier parent (the seeded Default
        Company Master when no parent is given). The library's owner is set to its
        name.
      tags: [libraries]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
                parentLibraryId:
                  type: string
                  format: uuid
                  description: Company-tier parent; defaults to the Default Company Master
      responses:
        '201':
          description: Client library created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/Library'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}:
    patch:
      operationId: renameLibrary
      summary: Rename a client library
      description: >
        Renames a client-tier library (name only; owner unchanged). Built-in
        reference and company libraries cannot be renamed.
      tags: [libraries]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
      responses:
        '200':
          description: Renamed library
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/Library'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}/specs:
    get:
      operationId: listLibrarySpecs
      summary: List the specs owned by a library
      description: >
        Returns the specs owned by the library, each with its paragraph node
        count and `withdrawnAt` tombstone, ordered by section. Withdrawn masters
        (ADR-030) are hidden by default; pass `includeWithdrawn=true` to surface
        them so a browse-and-restore flow can discover the spec UUID that
        `POST /specs/{id}/restore` needs.
      tags: [libraries]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema:
            type: string
            format: uuid
        - name: includeWithdrawn
          in: query
          required: false
          description: Include withdrawn masters (ADR-030), each with its withdrawnAt timestamp.
          schema:
            type: boolean
            default: false
        - name: discipline
          in: query
          required: false
          description: >
            Discipline key (from GET /disciplines) to filter the listing by, resolved
            under the library's mapping (ADR-065).
          schema:
            type: string
      responses:
        '200':
          description: Specs owned by the library
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/LibrarySpec'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}/reference-graph:
    get:
      operationId: getLibraryReferenceGraph
      summary: Library section-reference graph (nodes, edges, umbrella annotations)
      tags: [libraries]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema: { type: string, format: uuid }
        - name: includeAnchors
          in: query
          required: false
          description: Add capped per-edge paragraph anchor lists (see ReferenceGraph.anchorCap).
          schema: { type: boolean, default: false }
      responses:
        '200':
          description: Reference graph for the library
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/ReferenceGraph' }
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/import/jobs/{jobId}:
    get:
      operationId: getOnboardingJob
      summary: Poll a library-onboarding job
      tags: [libraries]
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Onboarding job status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/OnboardingJob'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}/import:
    post:
      operationId: importLibraryMaster
      summary: Onboard a master document into a library (async)
      description: >
        Upload a `.docx`, `.sec`, or `.txt` master document into the target
        ADR-015 library. Returns 202 with a `jobId`; poll
        `/libraries/import/jobs/{jobId}` for progress and the onboarding report.
        The pipeline parses (capturing source facts), persists the spec into the
        library, derives a style template (DOCX only), classifies editability
        using the library's convention profile or the built-in default, and
        summarizes hierarchy-inference confidence (ADR-055) into the report's
        hierarchy section. Importing a section/source the library already holds
        replaces that master's parsed content in place (`content_version` bumps) —
        and if the master was withdrawn, the re-import revives it (clears
        `withdrawnAt`, #415). Source bytes are discarded after parse (ADR-021).
        Rate-limited per IP (default
        10 requests/minute, shared with `POST /parse`; configurable, can be disabled).
      tags: [libraries]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Master document (.docx, .sec, or .txt). Max 10 MB.
      responses:
        '202':
          description: Onboarding job accepted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [jobId]
                        properties:
                          jobId:
                            type: string
                            format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}/conventions:
    get:
      operationId: getLibraryConvention
      summary: Get a library's convention profile (or inherited built-in)
      description: >
        Returns the library's own convention profile, or the inherited built-in
        industry default when the library has no profile of its own. The
        `meta.inherited` flag is `true` when the returned profile is the
        fallback built-in (`data.libraryId` is then `null`).
      tags: [conventions]
      parameters:
        - $ref: '#/components/parameters/ConventionLibraryId'
      responses:
        '200':
          description: Resolved convention profile with fallback flag
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data, meta]
                    properties:
                      data:
                        $ref: '#/components/schemas/EditingConvention'
                      meta:
                        type: object
                        required: [inherited]
                        properties:
                          inherited:
                            type: boolean
                            description: True when the profile is the inherited built-in default
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: putLibraryConvention
      summary: Create or replace a library's convention profile
      description: >
        Upserts the library's convention profile. Rules are shape-validated
        (malformed rules → 400). User-supplied `noteBanners` regexes are bounded
        for length and ReDoS safety at the write boundary (unsafe pattern → 422).
        Unknown rule keys round-trip unchanged (open schema).
      tags: [conventions]
      parameters:
        - $ref: '#/components/parameters/ConventionLibraryId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PutConventionBody'
      responses:
        '200':
          description: Convention profile created or replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/EditingConvention'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Unsafe or oversized `noteBanners` regex
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}/conventions/clone:
    post:
      operationId: cloneLibraryConvention
      summary: Clone a convention profile into a library
      description: >
        Clones a source profile (a built-in or another library's profile,
        identified by `sourceId`) into the target library, creating or replacing
        the target library's own profile.
      tags: [conventions]
      parameters:
        - $ref: '#/components/parameters/ConventionLibraryId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloneConventionBody'
      responses:
        '201':
          description: Convention profile cloned into the library
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/EditingConvention'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Unsafe or oversized `noteBanners` regex in the source profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /disciplines:
    get:
      operationId: listDisciplines
      summary: List disciplines with their resolved section mapping
      description: >
        Returns the discipline catalog, each discipline with its CSI division
        rules resolved for an optional `libraryId` (ADR-065). With no `libraryId`,
        or when the library has no rules of its own, the built-in default mapping
        is returned and `meta.inherited` is `true`. Unmapped disciplines carry an
        empty `rules` array.
      tags: [disciplines]
      parameters:
        - name: libraryId
          in: query
          required: false
          description: Resolve the mapping for this library (falls back to the built-in default).
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Resolved discipline catalog with fallback flag
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data, meta]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ResolvedDiscipline'
                      meta:
                        type: object
                        required: [inherited]
                        properties:
                          inherited:
                            type: boolean
                            description: True when the built-in default backs the result
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /libraries/{id}/disciplines:
    put:
      operationId: putLibraryDisciplines
      summary: Replace a library's discipline rule set
      description: >
        Replaces the library's discipline rule set wholesale (ADR-065). Each rule
        maps an inclusive CSI division range to a discipline `key` from
        GET /disciplines; ranges must not overlap. A malformed body → 400, an
        unknown discipline key → 422. The library then owns its mapping
        (`meta.inherited: false`). Clear the override with DELETE.
      tags: [disciplines]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetDisciplinesBody'
      responses:
        '200':
          description: Discipline rule set replaced; resolved catalog returned
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data, meta]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ResolvedDiscipline'
                      meta:
                        type: object
                        required: [inherited]
                        properties:
                          inherited:
                            type: boolean
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Unknown discipline key
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: clearLibraryDisciplines
      summary: Clear a library's discipline override
      description: >
        Removes the library's own discipline rules, reverting it to the built-in
        default (ADR-065). Idempotent: `data.cleared` is `true` when an override
        was removed, `false` when the library already had none.
      tags: [disciplines]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Override cleared (or already absent)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [cleared]
                        properties:
                          cleared:
                            type: boolean
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /revision-nomenclature-profiles:
    get:
      operationId: listRevisionNomenclatureProfiles
      summary: List revision nomenclature profiles
      description: >
        Returns built-in and project-scoped revision nomenclature profiles. Built-ins
        (`projectId: null`) provide default issuance/addendum/bulletin/CCD types.
      tags: [revision-nomenclature]
      responses:
        '200':
          description: Revision nomenclature profiles
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RevisionNomenclatureProfile'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/revision-nomenclature:
    get:
      operationId: getProjectRevisionNomenclature
      summary: Get a project's revision nomenclature profile
      description: >
        Returns the project's own revision nomenclature profile, or the inherited
        built-in default when the project has no profile. `meta.inherited` is true
        when `data.projectId` is null.
      tags: [revision-nomenclature]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Resolved revision nomenclature profile
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data, meta]
                    properties:
                      data:
                        $ref: '#/components/schemas/RevisionNomenclatureProfile'
                      meta:
                        type: object
                        required: [inherited]
                        properties:
                          inherited:
                            type: boolean
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: putProjectRevisionNomenclature
      summary: Create or replace a project's revision nomenclature profile
      tags: [revision-nomenclature]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PutRevisionNomenclatureBody'
      responses:
        '200':
          description: Project profile created or replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/RevisionNomenclatureProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: deleteProjectRevisionNomenclature
      summary: Delete a project's revision nomenclature override
      tags: [revision-nomenclature]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project override deleted; future reads inherit the built-in
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId]
                        properties:
                          projectId:
                            type: string
                            format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/revision-nomenclature/clone:
    post:
      operationId: cloneProjectRevisionNomenclature
      summary: Clone a revision nomenclature profile into a project
      tags: [revision-nomenclature]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloneRevisionNomenclatureBody'
      responses:
        '201':
          description: Revision nomenclature profile cloned into the project
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/RevisionNomenclatureProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/divisions/{division}/general-spec:
    get:
      operationId: getProjectDivisionGeneralSpec
      summary: Get division general-spec status for a project
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/Division'
      responses:
        '200':
          description: Division general-spec status
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/DivisionGeneralSpecResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      operationId: setProjectDivisionGeneralSpec
      summary: Manually set a project division general spec
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/Division'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetDivisionGeneralSpecBody'
      responses:
        '200':
          description: Manual division-general decision stored
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/DivisionGeneralSpecResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/specs:
    get:
      operationId: listProjectSpecs
      summary: List a project's specs with resolved disciplines
      description: >
        Returns the project's specs (its table-of-contents rows), each with its
        resolved `discipline` (ADR-065). Project copies belong to no single
        library, so disciplines resolve against the built-in default mapping.
        Pass `discipline` to keep only specs resolving to that discipline key.
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - name: discipline
          in: query
          required: false
          description: Discipline key (from GET /disciplines) to filter the listing by.
          schema:
            type: string
      responses:
        '200':
          description: Specs in the project TOC
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectSpec'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: addSectionToProject
      summary: Add a section to a project TOC (copy-on-derive clone)
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [section]
              properties:
                section:
                  type: string
                  pattern: '^\d{2} \d{2} \d{2}(\.\d{2}( \d{2})?)?$'
                  description: CSI section number (expanded shape, ADR-020)
      responses:
        '201':
          description: Section cloned into project
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [specId, section, position, source]
                        properties:
                          specId:
                            type: string
                            format: uuid
                            description: The project-owned clone's id (not the master's)
                          section:
                            type: string
                          position:
                            type: integer
                            description: 1-based TOC position assigned to the section
                          source:
                            type: object
                            required: [libraryId, name]
                            properties:
                              libraryId:
                                type: string
                                format: uuid
                              name:
                                type: string
                          shadowed:
                            type: array
                            description: Lower-priority sources that also hold this section
                            items:
                              type: object
                              required: [libraryId, name]
                              properties:
                                libraryId:
                                  type: string
                                  format: uuid
                                name:
                                  type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: 'Section already in project'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: 'Malformed section number, or no source library holds the section'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/specs/{specId}:
    delete:
      operationId: removeSectionFromProject
      summary: Remove a project-owned section copy (blocked if edited, unless force)
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - name: specId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: force
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Delete the copy even if it has project edits (content_version > 1)
      responses:
        '200':
          description: Section copy removed (dangling cross-references marked broken)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId, specId]
                        properties:
                          projectId:
                            type: string
                            format: uuid
                          specId:
                            type: string
                            format: uuid
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: 'Section has project edits (repeat with ?force=true) or belongs to a design package'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/specs/{specId}/references:
    get:
      operationId: getOutboundReferences
      summary: List cross-references emitted by a project spec
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - name: specId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Project-scoped outbound reference list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId, specId, references]
                        properties:
                          projectId:
                            type: string
                            format: uuid
                          specId:
                            type: string
                            format: uuid
                          references:
                            type: array
                            items:
                              $ref: '#/components/schemas/OutboundReference'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/references/broken:
    get:
      operationId: getBrokenRefs
      summary: List broken cross-references in a project
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Broken cross-reference list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BrokenRef'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/references/inbound:
    get:
      operationId: getInboundReferences
      summary: List project specs that reference a section
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - name: section
          in: query
          required: true
          schema:
            type: string
          description: CSI section number, URL-encoded when it contains spaces
      responses:
        '200':
          description: Project-scoped inbound reference list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId, section, references]
                        properties:
                          projectId:
                            type: string
                            format: uuid
                          section:
                            type: string
                          references:
                            type: array
                            items:
                              $ref: '#/components/schemas/InboundReference'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/packages:
    post:
      operationId: createPackage
      summary: Create a design package (named, ordered subset of the project TOC)
      tags: [packages]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
      responses:
        '201':
          description: Package created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/PackageSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Package name already exists in this project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'
    get:
      operationId: listPackages
      summary: List a project's design packages with ordered membership
      tags: [packages]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Package list with spec membership
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/PackageWithSpecs'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /packages/{id}/specs:
    put:
      operationId: setPackageSpecs
      summary: Replace a package's ordered spec membership
      tags: [packages]
      parameters:
        - name: id
          in: path
          required: true
          description: Package UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [specIds]
              properties:
                specIds:
                  type: array
                  uniqueItems: true
                  items:
                    type: string
                    format: uuid
                  description: >
                    Ordered list of spec UUIDs to assign to this package. Position
                    within the package equals array order (1-based). An empty array
                    clears the package. All specs must belong to the package's own
                    project TOC (ADR-015 D4).
      responses:
        '200':
          description: Package membership replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [packageId, specs]
                        properties:
                          packageId:
                            type: string
                            format: uuid
                          specs:
                            type: array
                            items:
                              $ref: '#/components/schemas/PackageSpecEntry'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: "Validation failed, or a spec is not in the package's project TOC"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /packages/{id}:
    delete:
      operationId: deletePackage
      summary: Delete a design package (membership rows cascade; specs survive)
      tags: [packages]
      parameters:
        - name: id
          in: path
          required: true
          description: Package UUID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Package deleted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [packageId]
                        properties:
                          packageId:
                            type: string
                            format: uuid
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /packages/{id}/revisions:
    get:
      operationId: listPackageRevisions
      summary: List a package's issued revisions (metadata only, ordered by sortOrder)
      description: >
        The package's issuance timeline — every issued revision as a light
        summary in sortOrder (the per-package issuance clock: 50% DD … IFC,
        addenda, bulletins). Metadata only; the frozen trees are read per
        revision via GET /revisions/{id}. Returns an empty array for a package
        that has issued nothing yet, and 404 for an unknown package.
      tags: [packages]
      parameters:
        - name: id
          in: path
          required: true
          description: Package UUID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Revision summaries in sortOrder
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RevisionSummary'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createPackageRevision
      summary: Issue an immutable revision snapshot of a package (ADR-015 D5)
      description: >
        Freezes the full SpecTree of every member section as JSONB inside one
        transaction — a reproducible point-in-time record of exactly what was
        issued. Post-issuance edits never alter the stored trees. Structured
        requests may persist an addendum comparison base via baseRevisionId.
      tags: [packages]
      parameters:
        - name: id
          in: path
          required: true
          description: Package UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/CreateRevisionLegacyBody'
                - $ref: '#/components/schemas/CreateRevisionStructuredBody'
      responses:
        '201':
          description: Revision issued
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/RevisionSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Revision label or sort order already exists for this package
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: >
            Validation failed, a member tree cannot be snapshotted losslessly,
            or parentRevisionId fails a custody rule (ADR-066): the parent
            revision does not exist, belongs to a different package, or
            already has a parent of its own (nesting depth cannot exceed 1);
            or baseRevisionId does not exist or belongs to another package.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /revisions/{id}:
    get:
      operationId: getRevision
      summary: Read an issued revision's frozen trees (validated on read)
      tags: [packages]
      parameters:
        - name: id
          in: path
          required: true
          description: Revision UUID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Frozen trees in membership order
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/RevisionWithTrees'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /revisions/{id}/generate:
    post:
      operationId: generateRevisionManual
      summary: Render an issued manual DOCX from frozen package revision snapshots
      description: >
        Renders the target package revision from `package_revision_specs.tree`, not
        live paragraph rows, so old issuances remain reproducible after database
        edits. Addendum mode uses the request `baseRevisionId` when supplied,
        otherwise the target revision's stored `baseRevisionId`; when neither
        is present the full issued manual is rendered. Addendum mode compares
        the target and base revision snapshots by section-level AST equality and
        emits only sections whose frozen trees differ, under an addendum cover
        listing the affected sections. Header/footer content is resolved and
        rendered per #481: the client → project → package → revision
        `header_footer_configs` chain is resolved for the target revision being
        rendered (never `baseRevisionId`, even in addendum mode) and rendered
        into every emitted spec section's own OOXML section. The addendum /
        front-matter cover section itself stays headerless (no header/footer
        reference in its `sectPr`), exactly as the project manual's cover does —
        only the emitted spec sections receive the resolved header/footer. A
        revision with zero configured layers anywhere in its chain falls back to
        the pre-#481 output — no header or footer, cover/front-matter revision
        identity unchanged.
      tags: [packages]
      parameters:
        - name: id
          in: path
          required: true
          description: Target revision UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                templateId:
                  type: string
                  format: uuid
                  description: >
                    Style template applied to every rendered section. Omitted →
                    the seeded UFGS-Default template; absent default → unstyled
                    200. Unknown id → 404.
                sectionNumberFormat:
                  type: string
                  enum: [canonical, dots, compact, spaced-compact]
                  default: canonical
                  description: >
                    Presentation format for generated section numbers in each
                    section title and confidently identified Section references.
                baseRevisionId:
                  type: string
                  format: uuid
                  description: >
                    Explicit base revision for addendum mode. Must belong to the
                    same package as the target revision. Overrides the target's
                    stored baseRevisionId; when both are omitted, renders the
                    full issued manual.
      responses:
        '200':
          description: DOCX manual file stream
          content:
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Invalid addendum comparison, or no changed sections to render
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /templates:
    post:
      operationId: createTemplate
      summary: Create a style template (no rules)
      tags: [templates]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Unique template name
                owner:
                  type: string
                  minLength: 1
                  description: Optional firm / owner label
                libraryId:
                  type: string
                  format: uuid
                  description: >
                    Optional owning library (#318). Omit for a built-in / global
                    template. A libraryId for a non-existent library → 404.
      responses:
        '201':
          description: Template created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateMeta'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
    get:
      operationId: listTemplates
      summary: List all style templates (metadata only, no rules)
      tags: [templates]
      responses:
        '200':
          description: Template list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TemplateMeta'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /templates/{id}:
    get:
      operationId: getTemplate
      summary: Retrieve a style template with its rules
      tags: [templates]
      parameters:
        - $ref: '#/components/parameters/TemplateId'
      responses:
        '200':
          description: Template with rules
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/Template'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    patch:
      operationId: patchTemplate
      summary: Update template metadata (name and/or owner)
      description: >
        At least one of `name` or `owner` must be present. Pass `owner: null`
        to clear the owner field.
      tags: [templates]
      parameters:
        - $ref: '#/components/parameters/TemplateId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                name:
                  type: string
                  minLength: 1
                owner:
                  type: [string, 'null']
                  minLength: 1
                  description: Pass null to clear the owner
      responses:
        '200':
          description: Updated template metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateMeta'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: deleteTemplate
      summary: Delete a style template and all its rules
      description: >
        A template assigned to any spec as its style source cannot be deleted
        (ON DELETE RESTRICT, #138) — the request returns 409 with the count of
        referencing specs. Clear the association on those specs first
        (DELETE /specs/{id}/style-source).
      tags: [templates]
      parameters:
        - $ref: '#/components/parameters/TemplateId'
      responses:
        '204':
          description: Template deleted
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Template is in use by one or more specs as a style source and cannot be deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /templates/{id}/rules:
    post:
      operationId: upsertTemplateRules
      summary: Bulk-upsert style rules for a template (transactional)
      description: >
        Replace (or add) rules for the given NodeTypes atomically. Existing rules
        for NodeTypes not in the request are left unchanged. All rules are
        validated before any write — one invalid rule rolls back all changes.
        Unknown OOXML property keys are preserved in `properties` (JSONB catchall).
      tags: [templates]
      parameters:
        - $ref: '#/components/parameters/TemplateId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rules]
              properties:
                rules:
                  type: array
                  minItems: 1
                  items:
                    $ref: '#/components/schemas/StyleRule'
      responses:
        '200':
          description: Template with updated rules
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/Template'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /templates/import:
    post:
      operationId: importTemplate
      summary: Derive a firm style template from a source-of-truth DOCX
      description: >
        Upload a `.docx` file to derive a reusable firm style template via
        per-NodeType consensus derivation (mode/median, consistency-wins-else-intent
        per §5 of the style fidelity program). Returns the persisted template and
        a `DerivationReport` for review.


        The raw DOCX is never stored — only the derived JSONB style definition is
        persisted (ADR-021). Unknown OOXML property keys are preserved in the
        `properties` object.


        **Theme-font caveat:** `asciiTheme` references are not yet resolved
        (issue #149). Documents that use theme fonts will derive without a
        concrete font family until theme-font support lands.


        Rate-limited per IP (default 10 requests/minute, shared with `POST /parse`; configurable, disable with `DISABLE_RATE_LIMIT`).
      tags: [templates]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, name]
              properties:
                file:
                  type: string
                  format: binary
                  description: Source-of-truth `.docx` file. Max 10 MB.
                name:
                  type: string
                  minLength: 1
                  description: Unique template name (used as the lookup key).
                owner:
                  type: string
                  minLength: 1
                  description: >
                    Optional firm / owner label (e.g. "ACME Architects"). Stored
                    as metadata only; has no effect on derivation.
      responses:
        '201':
          description: Template derived and persisted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [template, report]
                        properties:
                          template:
                            $ref: '#/components/schemas/Template'
                          report:
                            $ref: '#/components/schemas/DerivationReport'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /projects/{id}/required-sections:
    get:
      operationId: listBaselineRequiredSections
      summary: List a project's baseline required sections
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Baseline required-section list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/RequiredSection' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      operationId: putBaselineRequiredSections
      summary: Replace (or seed) a project's baseline required sections
      description: >
        `sections` and `seedFrom` are mutually exclusive. `seedFrom: toc` copies
        the project's TOC into an empty baseline. Re-replacing renumbers position.
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RequiredSectionsBody' }
      responses:
        '200':
          description: Updated baseline required-section list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/RequiredSection' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /projects/{id}/packages/{packageId}/required-sections:
    get:
      operationId: listPackageRequiredSections
      summary: List a design package's required sections
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: packageId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Package required-section list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/RequiredSection' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      operationId: putPackageRequiredSections
      summary: Replace (or seed) a design package's required sections
      description: >
        `sections` and `seedFrom` are mutually exclusive. `seedFrom: baseline`
        copies the project baseline; `seedFrom: toc` copies the project TOC;
        `seedFrom: {packageId}` copies another package. Re-replacing renumbers position.
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: packageId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RequiredSectionsBody' }
      responses:
        '200':
          description: Updated package required-section list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/RequiredSection' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /projects/{id}/coordination-report:
    get:
      operationId: getCoordinationReport
      summary: Project coordination report — required/present/reference + article-body consistency findings
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: packageId
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Coordination report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/CoordinationReport' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/reference-graph:
    get:
      operationId: getProjectReferenceGraph
      summary: Project section-reference graph (nodes, edges, umbrella annotations)
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - name: includeAnchors
          in: query
          required: false
          description: Add capped per-edge paragraph anchor lists (see ReferenceGraph.anchorCap).
          schema: { type: boolean, default: false }
      responses:
        '200':
          description: Reference graph for the project
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/ReferenceGraph' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/keynotes:
    get:
      operationId: getProjectKeynotes
      summary: Project keynote table export (tab-delimited, Revit format)
      description: >
        Exports the project's valid keynotes as the flat, tab-delimited text table
        BIM authoring tools consume (ADR-016 D3). Deterministic — a pure rendering
        of the project-filtered keynote query: a master keynote qualifies when its
        source library feeds the project and its target section is present in the
        project TOC (ADR-016 D2). Each row is `code<TAB>description` for a top-level
        keynote, or `code<TAB>description<TAB>parentCode` when it has a parent, so
        the keynote hierarchy is preserved. Rows are ordered by code; a code carried
        by two source libraries resolves to the higher-priority one. A project with
        no valid keynotes returns an empty body (200). Unknown project → 404.
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Tab-delimited keynote table
          content:
            text/plain:
              schema:
                type: string
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/revit-links:
    get:
      operationId: getProjectRevitLinks
      summary: Project Revit link inventory — element↔spec mapping read model
      description: >
        Read-only inventory (#103) over revit_parameter_mappings joined through
        paragraphs to project specs. Returns two pivots — byElement
        (element→sections) and bySpec (section→elements) — plus summary counts,
        including specsWithoutModelBacking. Optional revitInstanceId or specId
        query params narrow the two pivots; the summary is always project-wide.
        Note: unmappedElements is 0 under the current mappings-only substrate —
        model-placed-but-unmapped elements are unobservable without a model-element
        registry (ADR-029); the field is kept for forward contract stability.
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: revitInstanceId
          in: query
          required: false
          description: Filter the pivots to one Revit element instance id.
          schema: { type: string, minLength: 1 }
        - name: specId
          in: query
          required: false
          description: Filter the pivots to one project spec.
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Revit link inventory
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/RevitLinkInventory' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /reports/compare:
    post:
      operationId: postCompareReport
      summary: Cross-spec comparison matrix — align two live specs by origin or structure, with summary, differences filter, and optional baseline lens
      description: >
        Grounded, deterministic cross-spec comparison (ADR-047/053). Aligns exactly two
        live specs and returns a symmetric matrix: one row per aligned paragraph, one
        column per source, each cell the source's verbatim text or absent. Every present
        cell traces to a real specId + paragraph UUID; nothing is synthesized. Alignment
        is by resolved paragraph origin for clones of a shared master (project↔project,
        project↔master with behindBy drift) or by canonical structural address for
        independently-ingested specs of the same section — selected via `alignment`
        (default `auto`) and echoed as `alignedBy`. `include: differences` returns only
        non-identical rows; a `summary` rollup is always emitted over the full matrix.
        Optionally designate one source as the baseline to reframe cells as
        added/removed/modified/unchanged. A source id that is not a live spec (frozen
        package/revision ids) returns 404.
      tags: [reports]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CompareRequest' }
      responses:
        '200':
          description: Comparison report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/ComparisonReport' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/submittal-register:
    post:
      operationId: postSubmittalRegister
      summary: Product-driven submittal register for selected project specs
      description: >
        Builds a PM-facing register from selected project specs. Rows are driven by
        PART 2 - PRODUCTS product candidates; required submittal types come from
        each spec's PART 1 Submittals article; datasheet evidence comes from
        paragraph associations.
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SubmittalRegisterBody' }
      responses:
        '200':
          description: Product-driven submittal register
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/SubmittalRegister' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /specs/{id}/open-comments:
    get:
      operationId: getSpecOpenComments
      summary: Open (unclosed) Word comments in a spec
      description: >
        Lists the review comments in the spec that are NOT resolved (#256 C1) — the
        direct answer to "have all comments been closed?". A comment is closed when
        its runs are struck through OR its text ends in "Closed" (case/whitespace
        tolerant); everything else is reported here. A comment spanning N paragraphs
        surfaces once per covered paragraph.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '200':
          description: Open-comments report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/OpenCommentsReport' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/open-comments:
    get:
      operationId: getProjectOpenComments
      summary: Open (unclosed) Word comments across a project
      description: >
        Aggregates the unresolved review comments across every spec in the project,
        sorted by section (#256 C1). A comment is closed when its runs are struck
        through OR its text ends in "Closed"; everything else is reported here.
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Open-comments report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/OpenCommentsReport' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  # ─── Numbering profiles (#299) ───────────────────────────────────────────────

  /libraries/{id}/numbering-profiles:
    get:
      operationId: listNumberingProfiles
      summary: List numbering profiles for a library
      description: >
        Returns the library's own profiles plus the singleton built-in CSI
        Default (library_id IS NULL). Library rows appear first (ordered by
        created_at); the built-in is last.
      tags: [numbering-profiles]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Library UUID
      responses:
        '200':
          description: Numbering profiles (library-owned + CSI Default)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NumberingProfileRow'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: createNumberingProfile
      summary: Create a numbering profile for a library
      description: >
        Creates a new named numbering profile owned by the library. The `rules`
        object carries the NumberingProfile shape (tiers, numbering, styleLadder,
        articleIlvl). Library must exist (missing → 404). Each entry's `tier` is
        optional — the server derives it from ilvl + articleIlvl and rejects
        (422) a declared value that disagrees; articleIlvl is required once
        numbering or styleLadder is non-empty (ADR-067).
      tags: [numbering-profiles]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Library UUID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, rules]
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Display name for the profile.
                rules:
                  $ref: '#/components/schemas/NumberingProfile'
      responses:
        '201':
          description: Numbering profile created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/NumberingProfileRow'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /numbering-profiles/snapshot:
    post:
      operationId: snapshotNumberingProfile
      summary: Extract a numbering profile from a DOCX upload
      description: >
        Upload a `.docx` file and receive back its extracted `NumberingProfile`
        (tiers, numbering, styleLadder, articleIlvl) without persisting anything.
        Useful for previewing what profile would be inferred from a source-of-truth
        DOCX before creating a permanent profile row.


        Rate-limited per IP (default 10 requests/minute, shared with `POST /parse`; configurable, disable with `DISABLE_RATE_LIMIT`).
      tags: [numbering-profiles]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Source `.docx` file. Max 10 MB.
      responses:
        '200':
          description: Extracted numbering profile
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/NumberingProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /numbering-profiles/{id}:
    get:
      operationId: getNumberingProfile
      summary: Get a numbering profile by ID
      tags: [numbering-profiles]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Numbering profile UUID
      responses:
        '200':
          description: Numbering profile row
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/NumberingProfileRow'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    patch:
      operationId: patchNumberingProfile
      summary: Update a numbering profile (name and/or rules)
      description: >
        Partial update: omit a field to leave it unchanged. At least one of
        `name` or `rules` is required — an empty body is rejected (422).
        When `rules` is supplied, each entry's `tier` is optional —
        the server derives it from ilvl + articleIlvl and rejects (422) a
        declared value that disagrees; articleIlvl is required once numbering
        or styleLadder is non-empty (ADR-067). The built-in CSI Default
        (`libraryId: null`) is protected and cannot be modified (→ 409) — it
        is the fallback for every unassigned spec.
      tags: [numbering-profiles]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Numbering profile UUID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                rules:
                  $ref: '#/components/schemas/NumberingProfile'
      responses:
        '200':
          description: Updated numbering profile
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        $ref: '#/components/schemas/NumberingProfileRow'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: deleteNumberingProfile
      summary: Delete a numbering profile
      description: >
        Permanently deletes a library-owned profile. The built-in CSI Default
        (`libraryId: null`) is protected and cannot be deleted (→ 409). A
        profile assigned to any spec cannot be deleted (→ 409).
      tags: [numbering-profiles]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Numbering profile UUID
      responses:
        '204':
          description: Profile deleted
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /specs/{id}/numbering-profile:
    put:
      operationId: setSpecNumberingProfile
      summary: Assign a numbering profile to a spec
      description: >
        Associates an existing numbering profile with the spec so the parser
        uses it as a deterministic numbering override (#299). One profile per
        spec; re-assigning replaces the previous one. Both the spec and the
        profile must exist (either missing → 404, pre-checked; a profile
        concurrently deleted during assignment is also reported as 404, not a
        scope conflict, #366). Library scoping: the profile must be the built-in
        CSI Default or owned by the spec's own library; a profile from another
        library is rejected with 409.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [profileId]
              properties:
                profileId:
                  type: string
                  format: uuid
                  description: Numbering profile to assign to the spec.
      responses:
        '200':
          description: Numbering profile assigned
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [profileId, name]
                        properties:
                          profileId:
                            type: string
                            format: uuid
                          name:
                            type: string
                            description: Display name of the assigned profile.
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: clearSpecNumberingProfile
      summary: Clear a spec's numbering profile
      description: >
        Removes the spec→profile association, so the spec falls back to the built-in
        CSI Default when its structure is resolved. The spec must exist (missing → 404).
        Idempotent when no profile is currently assigned — the UPDATE still returns
        rowCount=1 if the spec exists.
      tags: [specs]
      parameters:
        - $ref: '#/components/parameters/SpecId'
      responses:
        '204':
          description: Numbering profile cleared
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # ─── Standards registry (#446, ADR-064) ──────────────────────────────────────
  /libraries/{id}/standards:
    get:
      operationId: getLibraryStandards
      summary: Library standards rollup (distinct cited standards + registry verdict)
      tags: [libraries]
      parameters:
        - name: id
          in: path
          required: true
          description: Library UUID
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Standards rollup for the library
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/StandardsRollup' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/standards:
    get:
      operationId: getProjectStandards
      summary: Project standards rollup (distinct cited standards + registry verdict)
      tags: [projects]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Standards rollup for the project
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/StandardsRollup' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /standards/{orgCode}/{standardCode}:
    put:
      operationId: recordStandardVerification
      summary: Record a standards verification verdict (upsert; server stamps last verified)
      description: >
        Upserts the registry record for one standard, keyed on (orgCode,
        standardCode). PUT-replace (ADR-064 §3): the body fully describes the
        verdict — omitted optional fields reset to null, status defaults to
        'unknown' — and the server stamps last_verified_at = now() on every write.
        orgCode is normalized to uppercase; standardCode is stored verbatim and
        must be percent-encoded in the path when it contains reserved characters
        (e.g. a slash in A653/A653M).
      tags: [standards]
      parameters:
        - name: orgCode
          in: path
          required: true
          description: Standards organization code (e.g. ASTM); normalized to uppercase.
          schema: { type: string, minLength: 1 }
        - name: standardCode
          in: path
          required: true
          description: Standard identifier within the org (e.g. C150). Percent-encode reserved chars.
          schema: { type: string, minLength: 1 }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/StandardVerificationBody' }
      responses:
        '200':
          description: The upserted registry record
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/StandardRecord' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /libraries/{id}/header-footer:
    get:
      operationId: getLibraryHeaderFooter
      summary: Get a client library's header/footer config
      description: >
        Reads the client library's own header/footer config (ADR-040). This is
        the top of the client -> project -> package -> revision precedence
        chain read by the resolve endpoints below; it is never itself
        inherited from anywhere.
      tags: [header-footer]
      parameters:
        - name: id
          in: path
          required: true
          description: Client library UUID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The library's header/footer config
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    put:
      operationId: putLibraryHeaderFooter
      summary: Create or replace a client library's header/footer config
      description: >
        Upsert (ADR-040). The body is the full `HeaderFooterComposition` —
        v1 (#208) `header`/`footer`/`style` fields, or v2 (#302)
        `variants`/`pageNumbering`/`raw` — validated and stored verbatim,
        including any unrecognized top-level extension keys.
      tags: [header-footer]
      parameters:
        - name: id
          in: path
          required: true
          description: Client library UUID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HeaderFooterComposition'
      responses:
        '200':
          description: Config created or replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404':
          description: Library not found
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '413': { $ref: '#/components/responses/PayloadTooLarge' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    delete:
      operationId: deleteLibraryHeaderFooter
      summary: Delete a client library's header/footer config
      tags: [header-footer]
      parameters:
        - name: id
          in: path
          required: true
          description: Client library UUID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Config deleted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [libraryId]
                        properties:
                          libraryId: { type: string, format: uuid }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404':
          description: Library not found, or the library has no header/footer config
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/header-footer:
    get:
      operationId: getProjectHeaderFooter
      summary: Get a project's own header/footer config override
      description: >
        Reads only the project's own override row — not the effective
        (inherited) config. Use `GET /projects/{id}/header-footer/resolved`
        for the merged client -> project result (ADR-040).
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: The project's header/footer config override
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    put:
      operationId: putProjectHeaderFooter
      summary: Create or replace a project's header/footer config override
      description: >
        Upsert (ADR-040). The body is the full `HeaderFooterComposition`; see
        `PUT /libraries/{id}/header-footer` for the shape contract.
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HeaderFooterComposition'
      responses:
        '200':
          description: Config created or replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '413': { $ref: '#/components/responses/PayloadTooLarge' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    delete:
      operationId: deleteProjectHeaderFooter
      summary: Delete a project's header/footer config override
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Override deleted; resolved reads fall back to the parent scope
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [projectId]
                        properties:
                          projectId: { type: string, format: uuid }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /packages/{id}/header-footer:
    get:
      operationId: getPackageHeaderFooter
      summary: Get a design package's own header/footer config override
      description: >
        Reads only the package's own override row. Use
        `GET /packages/{id}/header-footer/resolved` for the merged
        client -> project -> package result (ADR-040).
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/PackageId'
      responses:
        '200':
          description: The package's header/footer config override
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    put:
      operationId: putPackageHeaderFooter
      summary: Create or replace a design package's header/footer config override
      description: >
        Upsert (ADR-040). The body is the full `HeaderFooterComposition`; see
        `PUT /libraries/{id}/header-footer` for the shape contract.
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/PackageId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HeaderFooterComposition'
      responses:
        '200':
          description: Config created or replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '413': { $ref: '#/components/responses/PayloadTooLarge' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    delete:
      operationId: deletePackageHeaderFooter
      summary: Delete a design package's header/footer config override
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/PackageId'
      responses:
        '200':
          description: Override deleted; resolved reads fall back to the parent scope
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [packageId]
                        properties:
                          packageId: { type: string, format: uuid }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /revisions/{id}/header-footer:
    get:
      operationId: getRevisionHeaderFooter
      summary: Get an issued revision's own header/footer config override
      description: >
        Reads only the revision's own override row. Use
        `GET /revisions/{id}/header-footer/resolved` for the merged
        client -> project -> package -> revision result (ADR-040).
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/RevisionId'
      responses:
        '200':
          description: The revision's header/footer config override
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    put:
      operationId: putRevisionHeaderFooter
      summary: Create or replace an issued revision's header/footer config override
      description: >
        Upsert (ADR-040). The body is the full `HeaderFooterComposition`; see
        `PUT /libraries/{id}/header-footer` for the shape contract.
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/RevisionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HeaderFooterComposition'
      responses:
        '200':
          description: Config created or replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/HeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '413': { $ref: '#/components/responses/PayloadTooLarge' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalServerError' }
    delete:
      operationId: deleteRevisionHeaderFooter
      summary: Delete an issued revision's header/footer config override
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/RevisionId'
      responses:
        '200':
          description: Override deleted; resolved reads fall back to the parent scope
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data:
                        type: object
                        required: [revisionId]
                        properties:
                          revisionId: { type: string, format: uuid }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /projects/{id}/header-footer/resolved:
    get:
      operationId: resolveProjectHeaderFooter
      summary: Read the effective (merged) header/footer config for a project
      description: >
        Merges every contributing config row anchored above and at this
        project — client library, then the project itself — in that
        precedence order (later layers win per-key, ADR-040). `layers` lists
        every contributing row low-to-high precedence; the winning scope for
        any given key is `layers[layers.length - 1].scope`, whichever layer
        last touched it — no separate winning-scope field is synthesized.
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Resolved header/footer config
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/ResolvedHeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /packages/{id}/header-footer/resolved:
    get:
      operationId: resolvePackageHeaderFooter
      summary: Read the effective (merged) header/footer config for a design package
      description: >
        Merges every contributing config row anchored above and at this
        package — client library, project, then the package itself — in
        that precedence order (ADR-040). See
        `GET /projects/{id}/header-footer/resolved` for the layers/provenance
        contract.
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/PackageId'
      responses:
        '200':
          description: Resolved header/footer config
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/ResolvedHeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

  /revisions/{id}/header-footer/resolved:
    get:
      operationId: resolveRevisionHeaderFooter
      summary: Read the effective (merged) header/footer config for an issued revision
      description: >
        Merges every contributing config row anchored above and at this
        revision — client library, project, package, then the revision
        itself — in that precedence order (ADR-040). See
        `GET /projects/{id}/header-footer/resolved` for the layers/provenance
        contract.
      tags: [header-footer]
      parameters:
        - $ref: '#/components/parameters/RevisionId'
      responses:
        '200':
          description: Resolved header/footer config
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessResponse'
                  - type: object
                    required: [data]
                    properties:
                      data: { $ref: '#/components/schemas/ResolvedHeaderFooterConfig' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }

# ─── Components ───────────────────────────────────────────────────────────────

components:
  parameters:
    SpecId:
      name: id
      in: path
      required: true
      description: Spec UUID
      schema:
        type: string
        format: uuid
    NodeId:
      name: nodeId
      in: path
      required: true
      description: Paragraph (node) UUID
      schema:
        type: string
        format: uuid
    ProjectId:
      name: id
      in: path
      required: true
      description: Project UUID
      schema:
        type: string
        format: uuid
    ClientId:
      name: id
      in: path
      required: true
      description: Client UUID
      schema:
        type: string
        format: uuid
    LibraryId:
      name: libraryId
      in: path
      required: true
      description: Library UUID
      schema:
        type: string
        format: uuid
    PackageId:
      name: id
      in: path
      required: true
      description: Design package UUID
      schema:
        type: string
        format: uuid
    RevisionId:
      name: id
      in: path
      required: true
      description: Package revision UUID
      schema:
        type: string
        format: uuid
    Division:
      name: division
      in: path
      required: true
      description: Two-digit division number
      schema:
        type: string
        pattern: '^\d{2}$'
    TemplateId:
      name: id
      in: path
      required: true
      description: Template UUID
      schema:
        type: string
        format: uuid
    ConventionLibraryId:
      name: id
      in: path
      required: true
      description: Library UUID
      schema:
        type: string
        format: uuid

    AssociationId:
      name: associationId
      in: path
      required: true
      description: Paragraph association UUID
      schema:
        type: string
        format: uuid
    UserId:
      name: id
      in: path
      required: true
      description: User UUID
      schema:
        type: string
        format: uuid
  responses:
    BadRequest:
      description: Bad request — validation error or unsupported file type
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Forbidden — the resource exists but does not belong to the parent in the path
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Conflict — a resource with that identity already exists
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    WriteConflict:
      description: >
        Write rejected by the concurrency / edit gate (ADR-018): a stale
        `expectedVersion`, an archived spec, or a spec locked upstream. On a
        stale version the current version travels back as `currentVersion`.
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/ErrorResponse'
              - type: object
                properties:
                  currentVersion:
                    type: integer
                    description: Present on a stale-version conflict — the spec's current contentVersion.
    PayloadTooLarge:
      description: >
        Request body too large. The default REST body limit is 100KB; the
        four header/footer composition PUT routes
        (`/libraries|projects|packages|revisions/{id}/header-footer`) are
        dispatched to a larger, route-scoped limit of 7252652 bytes, sized to
        fit one image field at its own maxLength
        (`HEADER_FOOTER_JSON_BODY_LIMIT_BYTES`, derived from
        `MAX_IMAGE_BASE64_LENGTH`) plus headroom for the surrounding
        composition JSON — `{success: false, error: "payload too large"}`
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    UnprocessableEntity:
      description: Unprocessable — file parsed but semantically unusable
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TooManyRequests:
      description: >
        Rate limit exceeded (default 10 uploads per IP per minute; configurable) —
        `{success: false, error: "too many requests — please wait before uploading again"}`
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    # ── Shared field schemas ───────────────────────────────────────────────────

    ActorLabel:
      type: string
      minLength: 1
      maxLength: 200
      pattern: '[\s\S]*\S[\s\S]*'
      description: >
        Optional caller identity attributed to a paragraph-history write (#377);
        omitted falls back to a system sentinel. Resolves to a users.label and
        shares its contract: trimmed before resolution, whitespace-only rejected,
        and the 1-200 length bounds apply to the trimmed value.

    # ── Envelope shapes ────────────────────────────────────────────────────────

    SuccessResponse:
      type: object
      required: [success]
      properties:
        success:
          type: boolean
          enum: [true]

    ErrorResponse:
      type: object
      required: [success, error]
      properties:
        success:
          type: boolean
          enum: [false]
        error:
          type: string

    # ── Full-text search (ADR-062) ─────────────────────────────────────────────

    SearchHit:
      type: object
      description: One ranked paragraph match with stable navigation anchors.
      required: [paragraphId, text, nodeType, specId, specSection, specTitle, snippet, rank]
      properties:
        paragraphId:
          type: string
          format: uuid
          description: Paragraph UUID — the stable anchor for this hit.
        text:
          type: string
          description: Full paragraph text.
        nodeType:
          type: string
          description: >-
            CSI node type (part, article, pr1…pr7, note, continuation, or the
            object/objectText body-object types, #300).
        specId:
          type: string
          format: uuid
          description: UUID of the owning spec.
        specSection:
          type: string
          description: CSI section number of the owning spec, e.g. "07 84 00".
        specTitle:
          type: string
          description: Title of the owning spec.
        snippet:
          type: string
          description: ts_headline excerpt with matched terms wrapped in <mark>…</mark>.
        rank:
          type: number
          description: ts_rank_cd relevance score; 0 on the degenerate substring-fallback path.

    # ── Cross-spec comparison (reports) ────────────────────────────────────────

    CompareRequest:
      type: object
      required: [sources]
      properties:
        sources:
          type: array
          description: Exactly two live spec UUIDs (project or master) to compare.
          items: { type: string, format: uuid }
          minItems: 2
          maxItems: 2
        baseline:
          type: string
          format: uuid
          description: Optional — must be one of `sources`; projects a baseline lens over the matrix.
        alignment:
          type: string
          enum: [origin, structure, auto]
          default: auto
          description: >
            Row alignment strategy. `origin` keys on resolved paragraph origin
            (clones of a shared master); `structure` keys on the canonical structural
            address (independently-ingested specs of the same section); `auto`
            (default) uses origin when the sources share a cross-source origin key,
            structure when they share none but are the same section, else origin.
            The mode actually used is echoed as `alignedBy`.
        include:
          type: string
          enum: [all, differences]
          default: all
          description: >
            Row scope. `all` (default) returns the full matrix; `differences` returns
            only non-identical rows (modified / present-in-one). `summary` always
            reports full-matrix totals regardless of this filter.

    ComparisonColumn:
      type: object
      required: [specId, section, title]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
        title: { type: [string, 'null'] }

    ComparisonCell:
      description: A grounded cell — either a source's verbatim paragraph, or an absent sentinel.
      oneOf:
        - type: object
          required: [present, specId, paragraphUuid, text]
          properties:
            present: { type: boolean, enum: [true] }
            specId: { type: string, format: uuid }
            paragraphUuid: { type: string, format: uuid }
            text: { type: string }
        - type: object
          required: [present]
          properties:
            present: { type: boolean, enum: [false] }

    ComparisonMatrixRow:
      type: object
      required: [originId, cells]
      properties:
        originId:
          type: string
          description: >
            Row alignment key. A real paragraph UUID under `origin` alignment; a
            canonical structural-address string under `structure` (see `alignedBy`).
        cells:
          type: array
          description: Index-aligned to `columns`.
          items: { $ref: '#/components/schemas/ComparisonCell' }

    BaselineLensRow:
      type: object
      required: [originId, states]
      properties:
        originId: { type: string }
        states:
          type: array
          description: Index-aligned to `columns`.
          items:
            type: string
            enum: [baseline, unchanged, added, removed, modified, absent]

    BaselineLens:
      type: object
      required: [specId, rows]
      properties:
        specId: { type: string, format: uuid }
        rows:
          type: array
          items: { $ref: '#/components/schemas/BaselineLensRow' }

    DriftEntry:
      type: object
      required: [specId, behindBy]
      properties:
        specId: { type: string, format: uuid }
        behindBy:
          type: integer
          description: Versions this cloned source is behind its master parent (lineage chain).

    ComparisonSummary:
      type: object
      required: [rows, aligned, identical, differing, columns]
      description: Grounded rollup over the full matrix (before any `include` filter).
      properties:
        rows: { type: integer, description: Total aligned rows in the full matrix. }
        aligned: { type: integer, description: Rows present in ≥2 columns. }
        identical: { type: integer, description: Rows present in all columns with identical text. }
        differing: { type: integer, description: Rows that are not identical (rows − identical). }
        columns:
          type: array
          description: Per-column counts, index-aligned to `columns`.
          items:
            type: object
            required: [specId, present, onlyIn]
            properties:
              specId: { type: string, format: uuid }
              present: { type: integer, description: Rows where this column's cell is present. }
              onlyIn: { type: integer, description: Rows present ONLY in this column. }

    ComparisonReport:
      type: object
      required: [columns, rows, summary, alignedBy]
      properties:
        columns:
          type: array
          items: { $ref: '#/components/schemas/ComparisonColumn' }
        rows:
          type: array
          items: { $ref: '#/components/schemas/ComparisonMatrixRow' }
        alignedBy:
          type: string
          enum: [origin, structure]
          description: >
            The alignment mode actually used. Under `structure`,
            `ComparisonMatrixRow.originId` is a canonical structural-address string,
            not a paragraph UUID.
        summary:
          $ref: '#/components/schemas/ComparisonSummary'
        baseline:
          $ref: '#/components/schemas/BaselineLens'
        drift:
          type: array
          items: { $ref: '#/components/schemas/DriftEntry' }

    # ── Required Sections ──────────────────────────────────────────────────────

    RequiredSection:
      type: object
      required: [id, section, title, position]
      properties:
        id: { type: string, format: uuid }
        section:
          type: string
          description: Canonical CSI section number (ADR-020 expanded shape).
        title: { type: [string, 'null'] }
        position: { type: integer, minimum: 1 }

    RequiredSectionsBody:
      type: object
      description: >
        Provide either `sections` (explicit list) or `seedFrom` (copy an existing
        baseline/TOC/package) — not both.
      properties:
        sections:
          type: array
          items:
            type: object
            required: [section]
            properties:
              section:
                type: string
                description: Canonical CSI section number.
              title:
                type: string
                minLength: 1
        seedFrom:
          oneOf:
            - type: string
              enum: [baseline, toc]
            - type: object
              required: [packageId]
              properties:
                packageId: { type: string, format: uuid }
      # sections and seedFrom are mutually exclusive (server returns 422 if both
      # are present); neither is allowed too (an empty PUT clears the scope).
      not:
        required: [sections, seedFrom]

    # ── Coordination Report ────────────────────────────────────────────────────

    ReferenceGraph:
      type: object
      description: >
        One-call section-reference graph for a project or library (#447). Edges
        resolve scope-relative: a target section is resolved to the in-scope node
        that owns it, or null (dangling).
      required: [scope, nodes, edges, umbrella, anchorCap, notes]
      properties:
        scope:
          type: object
          required: [type, id]
          properties:
            type: { type: string, enum: [project, library] }
            id: { type: string, format: uuid }
        nodes:
          type: array
          items: { $ref: '#/components/schemas/ReferenceGraphNode' }
        edges:
          type: array
          items: { $ref: '#/components/schemas/ReferenceGraphEdge' }
        umbrella:
          type: array
          items: { $ref: '#/components/schemas/ReferenceGraphUmbrellaDivision' }
        anchorCap:
          type: integer
          description: Max paragraph anchors emitted per edge when includeAnchors=true.
        notes:
          type: array
          items: { type: string }
    ReferenceGraphNode:
      type: object
      required: [specId, section, title, division, isUmbrella]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
        title: { type: string }
        division: { type: [string, 'null'] }
        isUmbrella:
          type: boolean
          description: True when section equals "{division} 00 00".
    ReferenceGraphEdge:
      type: object
      required: [sourceSpecId, targetSection, targetSpecId, citationCount]
      properties:
        sourceSpecId: { type: string, format: uuid }
        targetSection: { type: string }
        targetSpecId:
          type: [string, 'null']
          format: uuid
          description: In-scope spec that owns targetSection, or null when dangling.
        citationCount: { type: integer }
        anchors:
          type: array
          description: Paragraph UUIDs citing the target (only when includeAnchors=true; capped at anchorCap).
          items: { type: string, format: uuid }
        anchorsTruncated:
          type: boolean
          description: True when citationCount exceeded anchorCap (only when includeAnchors=true).
    ReferenceGraphUmbrellaDivision:
      type: object
      required: [division, umbrellaSpecId, umbrellaPresent, notCalledOut]
      properties:
        division: { type: string }
        umbrellaSpecId: { type: [string, 'null'], format: uuid }
        umbrellaPresent: { type: boolean }
        notCalledOut:
          type: array
          description: In-scope specs in the division that never cite its "{division} 00 00" umbrella.
          items:
            type: object
            required: [specId, section]
            properties:
              specId: { type: string, format: uuid }
              section: { type: string }
    CoordinationReport:
      type: object
      required: [projectId, packageId, findings, summary, notes]
      properties:
        projectId: { type: string, format: uuid }
        packageId: { type: [string, 'null'], format: uuid }
        findings:
          type: array
          items: { $ref: '#/components/schemas/CoordinationFinding' }
        summary: { $ref: '#/components/schemas/CoordinationSummary' }
        notes:
          type: array
          items: { type: string }
    CoordinationSummary:
      type: object
      required:
        [
          requiredNotPresent,
          presentNotRequired,
          danglingRef,
          relatedListedNotCited,
          relatedCitedNotListed,
          standardCitedNotListed,
          productWithoutSubmittalType,
          submittalTypeWithoutProduct,
          productMissingDatasheet,
          impliedRelatedSection,
          umbrellaNotCalledOut,
          total,
        ]
      properties:
        requiredNotPresent: { type: integer }
        presentNotRequired: { type: integer }
        danglingRef: { type: integer }
        relatedListedNotCited: { type: integer }
        relatedCitedNotListed: { type: integer }
        standardCitedNotListed: { type: integer }
        productWithoutSubmittalType: { type: integer }
        submittalTypeWithoutProduct: { type: integer }
        productMissingDatasheet: { type: integer }
        impliedRelatedSection: { type: integer }
        umbrellaNotCalledOut: { type: integer }
        total: { type: integer }
    RevitLinkInventory:
      type: object
      description: Project-scoped Revit link inventory (#103) — element↔spec read model.
      required: [projectId, byElement, bySpec, summary]
      properties:
        projectId: { type: string, format: uuid }
        byElement:
          type: array
          description: One entry per Revit element, listing the specs it drives.
          items: { $ref: '#/components/schemas/RevitElementLinks' }
        bySpec:
          type: array
          description: One entry per present project spec, listing its linked elements.
          items: { $ref: '#/components/schemas/RevitSpecLinks' }
        summary: { $ref: '#/components/schemas/RevitLinkSummary' }
    RevitElementLinks:
      type: object
      required: [revitInstanceId, specs, linkCount]
      properties:
        revitInstanceId:
          type: string
          description: Revit element (family instance) id, stable per element within a model.
        specs:
          type: array
          items: { $ref: '#/components/schemas/RevitLinkedSpec' }
        linkCount:
          type: integer
          description: Number of mapping rows for this element within the project scope.
    RevitLinkedSpec:
      type: object
      required: [specId, section, title]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
        title: { type: string }
    RevitSpecLinks:
      type: object
      required: [specId, section, title, elements, linkCount]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
        title: { type: string }
        elements:
          type: array
          description: Distinct Revit element ids linked to this spec's paragraphs.
          items: { type: string }
        linkCount:
          type: integer
          description: Number of mapping rows targeting this spec within the project scope.
    RevitLinkSummary:
      type: object
      required:
        [
          elementCount,
          specCount,
          mappedSpecCount,
          specsWithoutModelBacking,
          unmappedElements,
          mappingCount,
        ]
      properties:
        elementCount:
          type: integer
          description: Distinct Revit elements with at least one link in the project.
        specCount:
          type: integer
          description: Total present project specs.
        mappedSpecCount:
          type: integer
          description: Project specs with at least one linked element.
        specsWithoutModelBacking:
          type: integer
          description: Project specs with no linked Revit element.
        unmappedElements:
          type: integer
          description: >-
            0 under the current mappings-only substrate — every mapped element
            resolves to a present project spec. A true count needs the deferred
            model-element registry (ADR-029 / ADR-049).
        mappingCount:
          type: integer
          description: Total in-scope mapping rows.
    CoordinationFinding:
      oneOf:
        - $ref: '#/components/schemas/FindingRequiredNotPresent'
        - $ref: '#/components/schemas/FindingPresentNotRequired'
        - $ref: '#/components/schemas/FindingDanglingRef'
        - $ref: '#/components/schemas/FindingRelatedListedNotCited'
        - $ref: '#/components/schemas/FindingRelatedCitedNotListed'
        - $ref: '#/components/schemas/FindingStandardCitedNotListed'
        - $ref: '#/components/schemas/FindingProductWithoutSubmittalType'
        - $ref: '#/components/schemas/FindingSubmittalTypeWithoutProduct'
        - $ref: '#/components/schemas/FindingProductMissingDatasheet'
        - $ref: '#/components/schemas/FindingImpliedRelatedSection'
        - $ref: '#/components/schemas/FindingUmbrellaNotCalledOut'
      discriminator:
        propertyName: type
        mapping:
          required_not_present: '#/components/schemas/FindingRequiredNotPresent'
          present_not_required: '#/components/schemas/FindingPresentNotRequired'
          dangling_ref: '#/components/schemas/FindingDanglingRef'
          related_listed_not_cited: '#/components/schemas/FindingRelatedListedNotCited'
          related_cited_not_listed: '#/components/schemas/FindingRelatedCitedNotListed'
          standard_cited_not_listed: '#/components/schemas/FindingStandardCitedNotListed'
          product_without_submittal_type: '#/components/schemas/FindingProductWithoutSubmittalType'
          submittal_type_without_product: '#/components/schemas/FindingSubmittalTypeWithoutProduct'
          product_missing_datasheet: '#/components/schemas/FindingProductMissingDatasheet'
          implied_related_section: '#/components/schemas/FindingImpliedRelatedSection'
          umbrella_not_called_out: '#/components/schemas/FindingUmbrellaNotCalledOut'
    FindingRequiredNotPresent:
      type: object
      required: [type, section, title, requiredId]
      properties:
        type: { type: string, enum: [required_not_present] }
        section: { type: string }
        title: { type: [string, 'null'] }
        requiredId: { type: string, format: uuid }
    FindingPresentNotRequired:
      type: object
      required: [type, section, specId, title]
      properties:
        type: { type: string, enum: [present_not_required] }
        section: { type: string }
        specId: { type: string, format: uuid }
        title: { type: string }
    FindingDanglingRef:
      type: object
      required:
        [
          type,
          refId,
          sourceSpecId,
          sourceSpecSection,
          sourceParagraphId,
          snippet,
          targetSpecSection,
          referenceText,
          availableFrom,
        ]
      properties:
        type: { type: string, enum: [dangling_ref] }
        refId: { type: string, format: uuid }
        sourceSpecId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        sourceParagraphId:
          type: string
          format: uuid
          description: Paragraph the dangling reference sits in — the paragraph-level locator
        snippet:
          type: string
          description: Short excerpt of that paragraph, centred on the matched reference
        targetSpecSection: { type: string }
        referenceText: { type: string }
        availableFrom:
          type: array
          items:
            type: object
            required: [libraryId, name]
            properties:
              libraryId: { type: string, format: uuid }
              name: { type: string }
    FindingRelatedListedNotCited:
      type: object
      description: >
        A3 — a section appears in this spec's Related Sections article but is never
        referenced anywhere else in its body. Likely an over-broad coordination list.
      required: [type, sourceSpecId, sourceSpecSection, sourceParagraphId, section]
      properties:
        type: { type: string, enum: [related_listed_not_cited] }
        sourceSpecId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        sourceParagraphId:
          type: string
          format: uuid
          description: Related Sections list paragraph that names the section — the paragraph-level locator
        section: { type: string, description: Canonical CSI number listed but not cited }
    FindingRelatedCitedNotListed:
      type: object
      description: >
        A2 — a section is cited in this spec's body but absent from its Related
        Sections article. The coordination list is missing a real dependency.
      required: [type, sourceSpecId, sourceSpecSection, sourceParagraphId, section]
      properties:
        type: { type: string, enum: [related_cited_not_listed] }
        sourceSpecId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        sourceParagraphId:
          type: string
          format: uuid
          description: Body paragraph that cites the section — the paragraph-level locator
        section: { type: string, description: Canonical CSI number cited but not listed }
    FindingStandardCitedNotListed:
      type: object
      description: >
        B2 — a code/standard (ASTM, NFPA, UL, …) is cited in this spec's body but
        absent from its References article. (The inverse, a listed-but-uncited
        standard, is intentionally NOT reported — #259 non-goal B1.)
      required: [type, sourceSpecId, sourceSpecSection, sourceParagraphId, standardCode]
      properties:
        type: { type: string, enum: [standard_cited_not_listed] }
        sourceSpecId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        sourceParagraphId:
          type: string
          format: uuid
          description: Body paragraph that cites the standard — the paragraph-level locator
        standardCode: { type: string, description: 'Normalized "ORG identifier", e.g. ASTM E814' }
    FindingImpliedRelatedSection:
      type: object
      description: >
        Advisory suggestion: this spec body contains a distinctive keyword from an
        in-scope section title, but that section is not already listed in this
        spec's Related Sections article. Suppressed for own-section, already-listed,
        generic, and overly common title keywords.
      required:
        [
          type,
          sourceSpecId,
          sourceSpecSection,
          sourceParagraphId,
          impliedSection,
          impliedTitle,
          matchedKeyword,
          confidence,
        ]
      properties:
        type: { type: string, enum: [implied_related_section] }
        sourceSpecId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        sourceParagraphId:
          type: string
          format: uuid
          description: Paragraph whose body text matched the implied section title keyword
        impliedSection:
          type: string
          description: Canonical CSI section number suggested for Related Sections
        impliedTitle: { type: string }
        matchedKeyword:
          type: string
          description: Normalized title keyword found in the source paragraph
        confidence:
          type: number
          minimum: 0
          maximum: 1
    FindingUmbrellaNotCalledOut:
      type: object
      description: >
        A subordinate section does not cite its division umbrella section
        (`DD 00 00`). Checked across every division present in scope (ADR-042);
        the coordination report notes list the divisions covered.
      required: [type, sourceSpecId, sourceSpecSection, umbrellaSpecSection]
      properties:
        type: { type: string, enum: [umbrella_not_called_out] }
        sourceSpecId: { type: string, format: uuid }
        sourceSpecSection:
          type: string
          description: Subordinate section missing the umbrella call-out.
        umbrellaSpecSection:
          type: string
          description: Expected division umbrella section number, e.g. `26 00 00`.

    FindingProductWithoutSubmittalType:
      type: object
      description: >
        A PART 2 product was specified, but the spec's PART 1 Submittals article
        yielded no recognized required submittal type for the product register.
      required: [type, specId, sourceSpecSection, productName, sourceParagraphId]
      properties:
        type: { type: string, enum: [product_without_submittal_type] }
        specId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        productName: { type: string }
        sourceParagraphId: { type: string, format: uuid }
    FindingSubmittalTypeWithoutProduct:
      type: object
      description: >
        A PART 1 Submittals article requires a submittal type, but no PART 2
        product candidate exists in that selected spec.
      required: [type, specId, sourceSpecSection, submittalType]
      properties:
        type: { type: string, enum: [submittal_type_without_product] }
        specId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        submittalType: { type: string }
    FindingProductMissingDatasheet:
      type: object
      description: >
        A PART 2 product is present but has no paragraph association that can
        serve as product-data/datasheet evidence.
      required: [type, specId, sourceSpecSection, productName, sourceParagraphId]
      properties:
        type: { type: string, enum: [product_missing_datasheet] }
        specId: { type: string, format: uuid }
        sourceSpecSection: { type: string }
        productName: { type: string }
        sourceParagraphId: { type: string, format: uuid }

    # ── Submittal Register (#263) ───────────────────────────────────────────────

    SubmittalRegisterBody:
      type: object
      required: [specIds]
      properties:
        specIds:
          type: array
          description: Project spec UUIDs selected for the register. Empty array is valid.
          items: { type: string, format: uuid }
          uniqueItems: true
    SubmittalRegister:
      type: object
      required: [projectId, specIds, rows, findings, summary, notes]
      properties:
        projectId: { type: string, format: uuid }
        specIds:
          type: array
          items: { type: string, format: uuid }
        rows:
          type: array
          items: { $ref: '#/components/schemas/SubmittalRegisterRow' }
        findings:
          type: array
          items: { $ref: '#/components/schemas/SubmittalRegisterFinding' }
        summary: { $ref: '#/components/schemas/SubmittalRegisterSummary' }
        notes:
          type: array
          items: { type: string }
    SubmittalRegisterRow:
      type: object
      required: [productName, requiredSubmittalTypes, datasheets, datasheetStatus, sources]
      properties:
        productName: { type: string }
        requiredSubmittalTypes:
          type: array
          items: { type: string }
        datasheets:
          type: array
          items: { $ref: '#/components/schemas/ParagraphAssociation' }
        datasheetStatus: { type: string, enum: [present, missing] }
        sources:
          type: array
          items: { $ref: '#/components/schemas/SubmittalRegisterSource' }
    SubmittalRegisterSource:
      type: object
      required: [specId, section, title, paragraphId, paragraphText]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
        title: { type: string }
        paragraphId: { type: string, format: uuid }
        paragraphText: { type: string }
    SubmittalRegisterFinding:
      oneOf:
        - $ref: '#/components/schemas/FindingProductWithoutSubmittalType'
        - $ref: '#/components/schemas/FindingSubmittalTypeWithoutProduct'
        - $ref: '#/components/schemas/FindingProductMissingDatasheet'
      discriminator:
        propertyName: type
        mapping:
          product_without_submittal_type: '#/components/schemas/FindingProductWithoutSubmittalType'
          submittal_type_without_product: '#/components/schemas/FindingSubmittalTypeWithoutProduct'
          product_missing_datasheet: '#/components/schemas/FindingProductMissingDatasheet'
    SubmittalRegisterSummary:
      type: object
      required:
        [
          rows,
          productWithoutSubmittalType,
          submittalTypeWithoutProduct,
          productMissingDatasheet,
          totalFindings,
        ]
      properties:
        rows: { type: integer }
        productWithoutSubmittalType: { type: integer }
        submittalTypeWithoutProduct: { type: integer }
        productMissingDatasheet: { type: integer }
        totalFindings: { type: integer }

    # ── Open comments (#262) ─────────────────────────────────────────────────────

    OpenCommentsScope:
      oneOf:
        - type: object
          required: [kind, specId]
          properties:
            kind: { type: string, enum: [spec] }
            specId: { type: string, format: uuid }
        - type: object
          required: [kind, projectId]
          properties:
            kind: { type: string, enum: [project] }
            projectId: { type: string, format: uuid }
      discriminator:
        propertyName: kind
    OpenComment:
      type: object
      required: [specId, specSection, paragraphId, author, text, anchor]
      properties:
        specId: { type: string, format: uuid }
        specSection: { type: string }
        paragraphId: { type: string, format: uuid }
        author: { type: string }
        text: { type: string }
        anchor: { $ref: '#/components/schemas/SourceTextSpan' }
    OpenCommentsSummary:
      type: object
      required: [open, total]
      properties:
        open: { type: integer }
        total:
          type: integer
          description: Total comment occurrences in scope (open + closed).
    OpenCommentsReport:
      type: object
      required: [scope, openComments, summary]
      properties:
        scope: { $ref: '#/components/schemas/OpenCommentsScope' }
        openComments:
          type: array
          items: { $ref: '#/components/schemas/OpenComment' }
        summary: { $ref: '#/components/schemas/OpenCommentsSummary' }

    # ── Parse ──────────────────────────────────────────────────────────────────

    ParseStage:
      type: string
      enum:
        - queued
        - running
        - extracting
        - numbering
        - styles
        - document
        - classifying
        - persisting
        - complete
        - failed

    ParseJob:
      type: object
      required: [jobId, status, progress, expiresAt]
      properties:
        jobId:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/ParseStage'
          description: >
            Overall job state. In practice takes `queued`, `running`, `complete`,
            or `failed`; the fine-grained pipeline step lives in `progress.stage`.
        progress:
          type: object
          required: [stage, pct]
          properties:
            stage:
              $ref: '#/components/schemas/ParseStage'
              description: Current pipeline step
            pct:
              type: number
              minimum: 0
              maximum: 100
        result:
          $ref: '#/components/schemas/ParseJobResult'
          description: Present once status is `complete`
        error:
          type: string
          description: Present once status is `failed`
        expiresAt:
          type: integer
          description: Job record expiry as a Unix epoch timestamp in milliseconds

    ParseJobResult:
      type: object
      required: [specId, section, title, nodeCount]
      properties:
        specId:
          type: string
          format: uuid
        section:
          type: string
          description: CSI section number, or "unknown" if not detected
        title:
          type: string
        nodeCount:
          type: integer
          description: Total nodes in the persisted paragraph tree
        capabilities:
          type: array
          items:
            type: string
          description: 'Parser capability flags (e.g. "read-only", "parse-warnings")'
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/ParseWarning'

    ParseWarning:
      type: object
      required: [type]
      properties:
        type:
          type: string
          enum:
            - root-continuation
            - empty-part
            - no-structure-found
            - unusual-part-count
            - non-conforming-part-numbering
            - core-metadata-unreadable
            - pdf-degraded-extraction
            - pdf-ocr-applied
            - pdf-ocr-low-confidence
            - pdf-ocr-unusable
            - pdf-font-encoding-remapped
            - pdf-font-encoding-unrecoverable
            - table-content-skipped
            - header-footer-content-skipped
            - body-drawing-skipped
        lineHint:
          type: string
          description: 'Source location hint (e.g. "line 12: <text>")'
        suggestion:
          type: string

    # ── Onboarding (O-8) ─────────────────────────────────────────────────────────

    OnboardingStage:
      type: string
      enum: [queued, running, parsing, persisting, deriving-style, classifying, complete, failed]

    EditabilityValue:
      type: string
      enum: [locked, editable, choice, note]

    EditabilitySummary:
      type: object
      required: [counts, lowConfidence]
      properties:
        counts:
          type: object
          required: [locked, editable, choice, note]
          properties:
            locked: { type: integer }
            editable: { type: integer }
            choice: { type: integer }
            note: { type: integer }
        lowConfidence:
          type: array
          description: Classified paragraphs below the machine-confidence review threshold.
          items:
            type: object
            required: [nodeId, value, confidence]
            properties:
              nodeId: { type: string, format: uuid }
              value: { $ref: '#/components/schemas/EditabilityValue' }
              confidence: { type: number, minimum: 0, maximum: 1 }

    HierarchySummary:
      type: object
      required: [counts, lowConfidence]
      properties:
        counts:
          type: object
          required: [scored, unscored, belowThreshold]
          properties:
            scored: { type: integer }
            unscored: { type: integer }
            belowThreshold: { type: integer }
        unscoredReason:
          type: string
          description: >-
            Present when unscored > 0 — why those paragraphs carry no score
            (explicit-structure source, pre-provenance parse, non-DOCX source,
            or manually inserted). Never folded into another bucket.
        lowConfidence:
          type: array
          description: Scored paragraphs below the hierarchy review threshold (0.6), worst-first.
          items:
            type: object
            required: [nodeId, nodeType, ilvl, confidence, evidence]
            properties:
              nodeId: { type: string, format: uuid }
              nodeType:
                type: string
                enum: [part, article, pr1, pr2, pr3, pr4, pr5, pr6, pr7]
              ilvl: { type: integer, minimum: 0, maximum: 8 }
              confidence: { type: number, minimum: 0, maximum: 1 }
              evidence:
                type: array
                items: { type: string }

    OnboardingReport:
      type: object
      required:
        [styleDerivation, styleSourceNeeded, headerFooter, editability, hierarchy, parseWarnings]
      properties:
        styleDerivation:
          oneOf:
            - $ref: '#/components/schemas/DerivationReport'
            - type: 'null'
          description: WT-3 consensus style derivation audit; null for non-DOCX masters.
        styleSourceNeeded:
          type: boolean
          description: True when no style template was derived (non-DOCX) — assign via O-12.
        headerFooter:
          oneOf:
            - $ref: '#/components/schemas/HeaderFooterComposition'
            - type: 'null'
          description: >
            Header/footer composition captured at parse time (ADR-040, #306);
            null when the source had none. Review draft only — not persisted
            here; save it (as-is or edited) via `PUT .../header-footer`
            (#208/#480).
        editability:
          $ref: '#/components/schemas/EditabilitySummary'
        hierarchy:
          $ref: '#/components/schemas/HierarchySummary'
        parseWarnings:
          type: array
          items:
            $ref: '#/components/schemas/ParseWarning'

    OnboardingJobResult:
      type: object
      required: [specId, section, title, libraryId, templateId, report]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
        title: { type: string }
        libraryId: { type: string, format: uuid }
        templateId:
          type: [string, 'null']
          description: Derived style template id (DOCX) or null (non-DOCX).
        report:
          $ref: '#/components/schemas/OnboardingReport'

    OnboardingJob:
      type: object
      required: [jobId, status, progress, expiresAt]
      properties:
        jobId: { type: string, format: uuid }
        status: { $ref: '#/components/schemas/OnboardingStage' }
        progress:
          type: object
          required: [stage, pct]
          properties:
            stage: { $ref: '#/components/schemas/OnboardingStage' }
            pct: { type: number, minimum: 0, maximum: 100 }
        result:
          $ref: '#/components/schemas/OnboardingJobResult'
        error: { type: string }
        expiresAt:
          type: integer
          description: Job record expiry as a Unix epoch timestamp in milliseconds.

    # ── Specs ──────────────────────────────────────────────────────────────────

    SignalConflict:
      type: object
      required: [signal, reportedIlvl, reportedNodeType]
      properties:
        signal:
          type: integer
          enum: [1, 2, 3, 4, 5]
          description: The losing inference signal that disagreed with the winner
        reportedIlvl:
          type: integer
          description: Normalized ilvl the losing signal reported (part=0 … pr7=8)
        reportedNodeType:
          type: string
          enum:
            [
              spec,
              part,
              article,
              pr1,
              pr2,
              pr3,
              pr4,
              pr5,
              pr6,
              pr7,
              note,
              continuation,
              object,
              objectText,
            ]

    SpecNodeInference:
      type: object
      required: [confidence, signalUsed, agreed, evidence]
      description: >-
        Hierarchy-inference confidence for this paragraph (ADR-055), derived at
        read time from persisted signal provenance and conflicts. Absent when
        the paragraph has no provenance (pre-provenance parse, non-DOCX source,
        or a manually inserted paragraph — no parse-time signals ran) or is
        non-structural (note/continuation).
      properties:
        confidence:
          type: number
          minimum: 0
          maximum: 1
        signalUsed:
          type: integer
          enum: [1, 2, 3, 4, 5]
          description: The winning inference signal (1 numbering.xml, 2 style chain, 3 document order, 4 text pattern, 5 indentation)
        agreed:
          type: array
          description: Signals whose vote matched the final resolved classification
          items:
            type: integer
            enum: [1, 2, 3, 4, 5]
        evidence:
          type: array
          description: Human-readable why-chain. Names signals, never source vendors.
          items:
            type: string

    ScoredParagraph:
      type: object
      required: [nodeId, nodeType, ilvl, label, preview, confidence, signalUsed, agreed, evidence]
      description: >-
        One scored structural paragraph in a GET /specs/{id}/hierarchy-report
        response (WS2, #424) — the same inference provenance as
        `SpecNodeInference`, plus the paragraph's identity, CSI label, and a
        text preview.
      properties:
        nodeId: { type: string, format: uuid }
        nodeType:
          type: string
          enum: [part, article, pr1, pr2, pr3, pr4, pr5, pr6, pr7]
        ilvl: { type: integer, minimum: 0, maximum: 8 }
        label:
          type: string
          description: CSI render label exactly as the markdown renderer would show it (e.g. "1.2", "PART 1").
        preview:
          type: string
          description: Paragraph text, truncated to 80 characters (no ellipsis — a straight prefix).
        confidence:
          type: number
          minimum: 0
          maximum: 1
        signalUsed:
          type: integer
          enum: [1, 2, 3, 4, 5]
          description: The winning inference signal (1 numbering.xml, 2 style chain, 3 document order, 4 text pattern, 5 indentation)
        agreed:
          type: array
          description: Signals whose vote matched the final resolved classification
          items:
            type: integer
            enum: [1, 2, 3, 4, 5]
        evidence:
          type: array
          description: Human-readable why-chain. Names signals, never source vendors.
          items:
            type: string
        conflicts:
          type: array
          description: Losing signals that disagreed with the winner. Omitted when there are none.
          items:
            $ref: '#/components/schemas/SignalConflict'

    HierarchyReport:
      type: object
      required: [counts, paragraphs]
      description: >-
        Per-paragraph hierarchy-inference scoring report (WS2, #424). `counts`
        and `unscoredReason` mirror `HierarchySummary` (the `hierarchy` field
        of `OnboardingReport`) exactly; `paragraphs` carries ALL scored nodes,
        worst confidence first, not just the below-threshold subset.
      properties:
        counts:
          type: object
          required: [scored, unscored, belowThreshold]
          properties:
            scored: { type: integer }
            unscored: { type: integer }
            belowThreshold: { type: integer }
        unscoredReason:
          type: string
          description: >-
            Present when unscored > 0 — why those paragraphs carry no score
            (explicit-structure source, pre-provenance parse, non-DOCX source,
            or manually inserted). Never folded into another bucket.
        paragraphs:
          type: array
          description: ALL scored structural paragraphs, worst confidence first.
          items:
            $ref: '#/components/schemas/ScoredParagraph'

    SourceTextSpan:
      type: array
      minItems: 2
      maxItems: 2
      prefixItems:
        - type: integer
          minimum: 0
        - type: integer
          minimum: 0

    SourceCommentFact:
      type: object
      required: [author, text, anchor, closed]
      properties:
        author:
          type: string
        text:
          type: string
        anchor:
          $ref: '#/components/schemas/SourceTextSpan'
        closed:
          type: boolean
          description: >
            True when the comment is resolved (#262): its runs are struck through
            OR its text ends in "Closed". For facts persisted before this field
            existed, it is derived from the trailing "Closed" suffix (the strike
            signal is unrecoverable, so such legacy facts read as open).

    SourceColorFact:
      type: object
      required: [color, coverage, spans]
      properties:
        color:
          type: string
        coverage:
          type: number
          minimum: 0
          maximum: 1
        spans:
          type: array
          items:
            $ref: '#/components/schemas/SourceTextSpan'

    SourceChoiceTokenFact:
      type: object
      required: [kind, options, span]
      properties:
        kind:
          type: string
          enum: [angle, bracket]
        options:
          type: array
          items:
            type: string
        span:
          $ref: '#/components/schemas/SourceTextSpan'

    SourceFacts:
      type: object
      description: >-
        Parser source facts for this paragraph. Present only when non-empty;
        unknown JSON keys are preserved for forward-compatible fact producers.
      additionalProperties: true
      properties:
        comments:
          type: array
          items:
            $ref: '#/components/schemas/SourceCommentFact'
        colors:
          type: array
          items:
            $ref: '#/components/schemas/SourceColorFact'
        choiceTokens:
          type: array
          items:
            $ref: '#/components/schemas/SourceChoiceTokenFact'
        banner:
          type: string
        vanish:
          type: boolean
          const: true

    SpecNode:
      type: object
      required: [id, type, text, children, meta]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          description: >-
            'object' and 'objectText' (#300) model a captured body-level
            table/text-box and its editable interior paragraph text — neither
            participates in CSI numbering or the 5-signal hierarchy engine.
          enum:
            [
              spec,
              part,
              article,
              pr1,
              pr2,
              pr3,
              pr4,
              pr5,
              pr6,
              pr7,
              note,
              continuation,
              object,
              objectText,
            ]
        text:
          type: string
        children:
          type: array
          items:
            $ref: '#/components/schemas/SpecNode'
        meta:
          type: object
          properties:
            vanish:
              type: boolean
              description: Hidden (w:vanish) paragraph — editorial note, not owner-facing
            source:
              type: string
              enum: [ufgs, arcat, cpi, unknown]
            revitParam:
              type: string
            baseVersion:
              type: integer
              minimum: 0
            conflicts:
              type: array
              description: >-
                DOCX inference signal disagreements for this paragraph. Present
                only when non-empty — the hierarchy level was ambiguous and the
                engine picked the highest-priority signal.
              items:
                $ref: '#/components/schemas/SignalConflict'
            sourceFacts:
              $ref: '#/components/schemas/SourceFacts'
            editability:
              $ref: '#/components/schemas/SpecNodeEditability'
            inference:
              $ref: '#/components/schemas/SpecNodeInference'
            associations:
              type: array
              description: >-
                External content links for this paragraph (#109). Present only
                when the paragraph has one or more associations.
              items:
                $ref: '#/components/schemas/ParagraphAssociation'
            articleRole:
              type: string
              description: >-
                Semantic CSI role of this article, derived from its heading
                (ADR-033). Present only on `article` nodes whose heading matches
                a recognized CSI role; absent for unknown/non-standard articles
                and all non-article nodes.
              enum:
                - summary
                - references
                - definitions
                - related-sections
                - submittals
                - quality-assurance
                - delivery-storage-handling
                - warranty
            object:
              description: >-
                Captured DOCX body object (#300, ADR-072) — a table or text
                box modeled as an opaque round-trip blob. Present only on
                `type: object` nodes.
              $ref: '#/components/schemas/ObjectMeta'

    ObjectMeta:
      type: object
      description: >-
        Metadata for a captured body-level object (#300, ADR-072): SpecR
        round-trips its OOXML byte-for-byte via `blob` rather than modeling
        its interior as CSI structure.
      required: [kind, floating, generation, blob]
      properties:
        kind:
          type: string
          enum: [table, textBox]
        floating:
          type: boolean
          description: Anchored/wrapped (true) vs. inline (false) placement.
        generation:
          type: string
          description: The OOXML drawing generation the object was authored in.
          enum: [drawingml, vml]
        rows:
          type: integer
          minimum: 1
          description: Table row count (table kind only).
        columns:
          type: integer
          minimum: 1
          description: Table column count (table kind only).
        blob:
          type: array
          description: >-
            The object's own top-level OOXML node(s), in document order, as
            fast-xml-parser `preserveOrder: true` JSON — an opaque capture
            format, never interpreted as modeled CSI structure. Always
            non-empty.
          minItems: 1
          items:
            type: object
            additionalProperties: true

    SpecTree:
      type: object
      required: [id, section, title, parts]
      properties:
        id:
          type: string
          format: uuid
        section:
          type: string
          description: 'CSI section number (e.g. "09 91 26"), or "unknown" if not detected'
        title:
          type: string
        parts:
          type: array
          items:
            $ref: '#/components/schemas/SpecNode'
        styleSource:
          description: >
            Manually assigned style template (#138), or null when none is set.
            Only present on GET /specs/{id}.
          oneOf:
            - $ref: '#/components/schemas/StyleSource'
            - type: 'null'
        onboardingStatus:
          description: >
            Whether a human has reviewed the machine's first pass (ADR-022 D6).
            Only present on GET /specs/{id}.
          type: string
          enum: [review, active]
        withdrawnAt:
          type: [string, 'null']
          format: date-time
          description: >
            Soft-delete (withdraw) tombstone (ADR-030): ISO-8601 when the master
            is withdrawn, `null` when active. A withdrawn master stays GET-able so
            lineage/history resolves; only listings/resolution hide it. Only
            present on GET /specs/{id}.
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/ParseWarning'
        hiddenTables:
          type: array
          items:
            $ref: '#/components/schemas/RetainedTable'
          description: >
            Hidden DOCX tables (all evidence-bearing cell paragraphs vanish)
            retained out-of-band for future change-management (ADR-038).
            Absent when the source has no hidden tables.
        headerFooter:
          description: >
            Captured DOCX header/footer composition (ADR-040, #306), parsed
            straight from the source document. Parse-output only — not
            persisted; separate from the header/footer config CRUD surface
            (`GET/PUT/DELETE .../header-footer`, #476). Absent when the
            source has no headers or footers.
          allOf:
            - $ref: '#/components/schemas/HeaderFooterComposition'

    StyleSource:
      type: object
      required: [templateId, templateName]
      properties:
        templateId:
          type: string
          format: uuid
        templateName:
          type: string

    RetainedTable:
      type: object
      required: [rows]
      description: >
        A DOCX table classified as hidden and retained out-of-band for future
        change-management (ADR-038). Rows are preserved as plain-text grids —
        no per-cell structure inference.
      properties:
        rows:
          type: array
          items:
            type: array
            items:
              type: string

    SpecSummary:
      type: object
      required: [specId, title, section]
      properties:
        specId:
          type: string
          format: uuid
        title:
          type: string
        section:
          type: string

    ParagraphDiff:
      type: object
      required: [uuid, text, index]
      properties:
        uuid:
          type: string
          format: uuid
          description: Synthesized UUID for a paragraph added outside known controls
        text:
          type: string
        index:
          type: integer
          minimum: 0
          description: Document-order index in the returned DOCX
        afterUuid:
          type: string
          format: uuid
          description: >
            Nearest preceding controlled-paragraph UUID in document order,
            omitted when the addition precedes every known control or sits in a
            table cell (which never anchors). Anchors where `/specs/{id}/merge`
            inserts this paragraph if the UUID is accepted; an addition with no
            anchor, or one whose anchor is a structural part/article/note node,
            cannot be applied (400).

    ModifiedDiff:
      type: object
      required: [uuid, base, theirs, ours]
      properties:
        uuid:
          type: string
          format: uuid
        base:
          type: string
          description: Base snapshot text
        theirs:
          type: string
          description: Returned DOCX text
        ours:
          type: string
          description: Current DB text

    ObjectStructureFingerprint:
      type: object
      required: [kind, hash]
      description: >
        A text-blind, structure-sensitive summary of one body-level table/text
        box's OOXML shape (#520) — editing cell/paragraph text never changes
        it, but adding/removing a row, column, or nested element does.
      properties:
        kind:
          type: string
          enum: [table, textBox]
        rows:
          type: integer
          minimum: 0
          description: Table-only grid row count; absent for a textBox kind.
        columns:
          type: integer
          minimum: 0
          description: Table-only grid column count; absent for a textBox kind.
        hash:
          type: string
          description: Digest of the object's structural shape, opaque to clients.

    ObjectConflictDiff:
      type: object
      required: [objectId, affectedUuids, base, theirs]
      description: >
        One atomic structural conflict on a body-level table/text box (#520):
        the base-side object's structure diverges from the matching object in
        the returned DOCX (`theirs`) — a row/column added or removed, a
        table/text-box kind change, etc. Reported once per object rather than
        leaking into per-child-paragraph noise; `affectedUuids` (the object's
        base-side interior child anchors) never also appear in
        `modified`/`deleted`/`conflicts` above. Detection-only — accepting
        `objectId` or any `affectedUuids` entry is always rejected 400; the
        table/text box must be resolved by hand.
      properties:
        objectId:
          type: string
          format: uuid
          description: The base-side object row's own id.
        affectedUuids:
          type: array
          items:
            type: string
            format: uuid
          description: The object's base-side interior child paragraph anchors.
        base:
          $ref: '#/components/schemas/ObjectStructureFingerprint'
        theirs:
          $ref: '#/components/schemas/ObjectStructureFingerprint'

    DiffResult:
      type: object
      required: [added, modified, deleted, conflicts, objectConflicts, warnings]
      properties:
        added:
          type: array
          items:
            $ref: '#/components/schemas/ParagraphDiff'
        modified:
          type: array
          items:
            $ref: '#/components/schemas/ModifiedDiff'
        deleted:
          type: array
          items:
            type: string
            format: uuid
        conflicts:
          type: array
          description: Same shape as modified; present when ours and theirs both changed
          items:
            $ref: '#/components/schemas/ModifiedDiff'
        objectConflicts:
          type: array
          description: >
            Atomic structural conflicts on body-level tables/text boxes
            (#520). Never auto-mergeable — see ObjectConflictDiff.
          items:
            $ref: '#/components/schemas/ObjectConflictDiff'
        warnings:
          type: array
          items:
            type: string

    MergeRequest:
      type: object
      required: [accept, diff]
      additionalProperties: false
      properties:
        accept:
          type: array
          items:
            type: string
            format: uuid
          description: >
            UUIDs from `diff.modified`, `diff.conflicts`, `diff.added`, or
            `diff.deleted` to apply.
        diff:
          $ref: '#/components/schemas/DiffResult'
        expectedVersion:
          type: integer
          minimum: 1
          description: >
            Optional optimistic-concurrency precondition (ADR-018) — the spec
            `contentVersion` the redline was diffed against. Stale → 409.
        actorLabel:
          $ref: '#/components/schemas/ActorLabel'

    LockState:
      type: object
      required: [holder, acquiredAt, expiresAt]
      description: An advisory soft lock on a spec (ADR-018 D2).
      properties:
        holder:
          type: string
          description: Caller identity label holding the lock.
        acquiredAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time

    MergeResult:
      type: object
      required: [applied, rejected]
      properties:
        applied:
          type: integer
          minimum: 0
          description: Number of paragraph rows changed
        rejected:
          type: integer
          minimum: 0
          description: Number of diff entries omitted from `accept`

    LineageHop:
      type: object
      required: [specId, scope, name, contentVersion, originVersion, behindBy]
      description: >
        One node in the derivation chain. `originVersion` and `behindBy` are null
        on the root (it has no parent to drift behind).
      properties:
        specId:
          type: string
          format: uuid
        scope:
          type: string
          enum: [library, project]
          description: Whether this spec lives in a library or a project
        name:
          type: string
          description: Library or project name that owns this spec
        contentVersion:
          type: integer
          description: Current edit generation of this spec
        originVersion:
          type: [integer, 'null']
          description: Content version of the parent this spec was cloned from; null on root
        behindBy:
          type: [integer, 'null']
          description: >
            Parent's current contentVersion minus this copy's originVersion —
            how many parent edits this copy has not yet seen; null on root

    SpecLineage:
      type: object
      required: [chain, originMeta]
      properties:
        chain:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/LineageHop'
          description: >
            Derivation chain, leaf-first. `chain[0]` is the requested spec;
            the last entry is the root library master.
        originMeta:
          description: >
            Ingest provenance of the chain root (filename, sha256, loader);
            null when the root was not file-ingested.
          oneOf:
            - type: object
              required: [filename, sha256, loader]
              properties:
                filename:
                  type: string
                sha256:
                  type: string
                  minLength: 64
                  maxLength: 64
                loader:
                  type: string
            - type: 'null'

    DivisionGeneralSpecRef:
      type: object
      required: [specId, section, title]
      properties:
        specId:
          type: string
          format: uuid
        section:
          type: string
        title:
          type: string

    DivisionGeneralCandidate:
      allOf:
        - $ref: '#/components/schemas/DivisionGeneralSpecRef'
        - type: object
          required: [rank, reason, confidence]
          properties:
            rank:
              type: integer
              minimum: 1
            reason:
              type: string
              enum: [title_keyword, first_in_division]
              description: Why this spec is listed as an advisory fallback candidate
            confidence:
              type: string
              enum: [medium, low]

    DivisionGeneralSpecResult:
      type: object
      required:
        - scope
        - ownerId
        - division
        - expectedSection
        - status
        - generalSpec
        - detectionMethod
        - notes
        - message
        - candidates
      properties:
        scope:
          type: string
          enum: [library, project]
        ownerId:
          type: string
          format: uuid
        division:
          type: string
          pattern: '^\d{2}$'
        expectedSection:
          type: string
          description: Exact section number that would be assigned automatically, e.g. "27 00 00"
        status:
          type: string
          enum: [resolved, missing, not_applicable]
        generalSpec:
          oneOf:
            - $ref: '#/components/schemas/DivisionGeneralSpecRef'
            - type: 'null'
        detectionMethod:
          type: [string, 'null']
          enum: [exact_section, manual, null]
        notes:
          type: [string, 'null']
        message:
          type: string
        candidates:
          type: array
          description: Advisory only; never treated as inherited until manually selected
          items:
            $ref: '#/components/schemas/DivisionGeneralCandidate'

    SetDivisionGeneralSpecBody:
      description: 'Provide exactly one of `generalSpecId` or `status: not_applicable`.'
      oneOf:
        - type: object
          required: [generalSpecId]
          additionalProperties: false
          properties:
            generalSpecId:
              type: string
              format: uuid
              description: Same-scope spec in the requested division to use as the general spec
            notes:
              type: string
              minLength: 1
        - type: object
          required: [status]
          additionalProperties: false
          properties:
            status:
              type: string
              enum: [not_applicable]
              description: Records that no in-scope general requirements spec is issued here
            notes:
              type: string
              minLength: 1

    # ── Projects ───────────────────────────────────────────────────────────────

    ProjectListItem:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string

    ClientSummary:
      type: object
      required: [id, name, libraryId, sectionNumberFormat, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        libraryId:
          type: [string, 'null']
          format: uuid
          description: The client's client-tier master library (a link, not a merge), or null
        sectionNumberFormat:
          type: string
          enum: [canonical, dots, compact, spaced-compact]
          description: Firm-tier default used when an associated project has no override
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    UserSummary:
      type: object
      required: [id, label, createdAt]
      properties:
        id:
          type: string
          format: uuid
        label:
          type: string
          description: Case-sensitive, exact-match actor identity (ADR-052 D6)
        createdAt:
          type: string
          format: date-time

    ClientDetail:
      allOf:
        - $ref: '#/components/schemas/ClientSummary'
        - type: object
          required: [projects]
          properties:
            projects:
              type: array
              description: The client's associated active projects
              items:
                $ref: '#/components/schemas/ProjectSummary'

    ProjectSummary:
      type: object
      required: [projectId, name, description, clientId, clientName, sources]
      properties:
        projectId:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: [string, 'null']
        clientId:
          type: [string, 'null']
          format: uuid
          description: Associated client (ADR-054), or null when the project has no client
        clientName:
          type: [string, 'null']
          description: Associated client's name (denormalized for lists), or null
        sources:
          type: array
          description: Ordered source libraries (priority ascending)
          items:
            type: object
            required: [libraryId, name, tier, priority]
            properties:
              libraryId:
                type: string
                format: uuid
              name:
                type: string
              tier:
                type: string
                enum: [company, client]
              priority:
                type: integer
                minimum: 1

    ProjectWithToc:
      type: object
      required:
        [projectId, name, description, sources, toc, deletedAt, deletedBy, sectionNumberFormat]
      properties:
        projectId:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: [string, 'null']
        sources:
          type: array
          description: Ordered source libraries (priority ascending)
          items:
            type: object
            required: [libraryId, name, tier, priority]
            properties:
              libraryId:
                type: string
                format: uuid
              name:
                type: string
              tier:
                type: string
                enum: [company, client]
              priority:
                type: integer
                minimum: 1
        toc:
          type: array
          items:
            type: object
            required: [specId, section, title, position]
            properties:
              specId:
                type: string
                format: uuid
              section:
                type: string
              title:
                type: string
              position:
                type: integer
                description: 1-based TOC ordering
        deletedAt:
          type: [string, 'null']
          format: date-time
          description: >
            Soft-delete tombstone (ADR-031). `null` on an active project; an
            ISO-8601 timestamp once soft-deleted.
        deletedBy:
          type: [string, 'null']
          description: >
            Caller-supplied actor that soft-deleted the project (ADR-031). `null`
            while active.
        sectionNumberFormat:
          type: string
          enum: [canonical, dots, compact, spaced-compact]
          description: >
            Effective section-number display format for generate requests: the
            project override, then its client's firm default, then `canonical`
            ("09 91 00").

    BrokenRef:
      type: object
      required:
        [
          refId,
          sourceSpecId,
          sourceSpecSection,
          sourceParagraphId,
          snippet,
          targetSpecSection,
          referenceText,
          availableFrom,
        ]
      properties:
        refId:
          type: string
          format: uuid
        sourceSpecId:
          type: string
          format: uuid
        sourceSpecSection:
          type: string
          description: CSI section number of the spec containing the reference
        sourceParagraphId:
          type: string
          format: uuid
          description: Paragraph the reference sits in — the paragraph-level locator
        snippet:
          type: string
          description: Short excerpt of that paragraph, centred on the matched reference
        targetSpecSection:
          type: [string, 'null']
          description: Section number the reference points at (as written in the source document)
        referenceText:
          type: string
        availableFrom:
          type: array
          description: >
            Project source libraries that hold the missing target section — an
            actionable "add this section" advisory. Priority order.
          items:
            type: object
            required: [libraryId, name]
            properties:
              libraryId:
                type: string
                format: uuid
              name:
                type: string

    OutboundReference:
      type: object
      required:
        [
          sourceSpecId,
          sourceParagraphId,
          referenceText,
          targetSection,
          targetSpecId,
          isResolved,
          isBroken,
        ]
      properties:
        sourceSpecId:
          type: string
          format: uuid
        sourceParagraphId:
          type: string
          format: uuid
          description: Paragraph the reference sits in — the paragraph-level locator
        referenceText:
          type: string
        targetSection:
          type: [string, 'null']
          description: Section number this reference points at, if the reference targets a section
        targetSpecId:
          type: [string, 'null']
          format: uuid
        isResolved:
          type: boolean
        isBroken:
          type: boolean

    InboundReference:
      type: object
      required:
        [
          sourceSpecId,
          sourceSection,
          sourceTitle,
          sourceParagraphId,
          referenceText,
          isResolved,
          isBroken,
        ]
      properties:
        sourceSpecId:
          type: string
          format: uuid
        sourceSection:
          type: string
          description: CSI section number of the spec containing the reference
        sourceTitle:
          type: string
        sourceParagraphId:
          type: string
          format: uuid
        referenceText:
          type: string
        isResolved:
          type: boolean
        isBroken:
          type: boolean

    # ── Packages ───────────────────────────────────────────────────────────────

    PackageSummary:
      type: object
      required: [packageId, projectId, name, position]
      properties:
        packageId:
          type: string
          format: uuid
        projectId:
          type: string
          format: uuid
        name:
          type: string
        position:
          type: integer
          description: 1-based order of the package within its project

    PackageSpecEntry:
      type: object
      required: [specId, section, title, position]
      properties:
        specId:
          type: string
          format: uuid
        section:
          type: string
        title:
          type: string
        position:
          type: integer
          description: 1-based order of the spec within the package

    PackageWithSpecs:
      allOf:
        - $ref: '#/components/schemas/PackageSummary'
        - type: object
          required: [specs]
          properties:
            specs:
              type: array
              items:
                $ref: '#/components/schemas/PackageSpecEntry'

    RevisionAttributes:
      type: object
      description: >
        Open JSONB attributes for a profile-defined revision type. Known common
        keys include `number`, `title`, and `phase`; unknown keys are preserved.
      additionalProperties: true
      properties:
        number:
          oneOf:
            - type: string
            - type: number
        title:
          type: string
        phase:
          type: string

    CreateRevisionLegacyBody:
      type: object
      required: [label]
      properties:
        label:
          type: string
          minLength: 1
          description: Legacy freeform label; still accepted for backward compatibility

    CreateRevisionStructuredBody:
      type: object
      required: [type]
      properties:
        type:
          type: string
          minLength: 1
          description: String key defined by the resolved revision nomenclature profile
        date:
          type: string
          format: date
        sortOrder:
          type: integer
          minimum: 1
        attributes:
          $ref: '#/components/schemas/RevisionAttributes'
        parentRevisionId:
          type: string
          format: uuid
          description: >
            UUID of the revision this one was issued from (ADR-066 custody).
            Must belong to the same package and itself be a root revision —
            nesting depth cannot exceed 1. Omit for a root revision.
        baseRevisionId:
          type: string
          format: uuid
          description: >
            UUID of the revision used as the immutable addendum comparison
            base (ADR-066). Must belong to the same package. Omit when this
            issuance has no stored comparison lineage.

    RevisionSummary:
      type: object
      required:
        [
          revisionId,
          packageId,
          label,
          displayName,
          type,
          date,
          sortOrder,
          number,
          attributes,
          issuedAt,
          specCount,
          parentRevisionId,
          baseRevisionId,
        ]
      properties:
        revisionId:
          type: string
          format: uuid
        packageId:
          type: string
          format: uuid
        label:
          type: string
          description: Backward-compatible display label, unique per package
        displayName:
          type: string
          description: Profile-rendered display name; #102 consumes this directly
        type:
          type: string
          description: Profile-defined revision type key
        date:
          type: string
          format: date
        sortOrder:
          type: integer
          minimum: 1
          description: Queryable ordering key within the package
        number:
          type: [string, 'null']
          description: Profile-rendered revision number, or null when not applicable
        attributes:
          $ref: '#/components/schemas/RevisionAttributes'
        issuedAt:
          type: string
          format: date-time
        specCount:
          type: integer
          description: Number of member sections frozen in this revision
        parentRevisionId:
          type: [string, 'null']
          format: uuid
          description: >
            UUID of the revision this one was issued from (ADR-066 custody),
            or null for a root revision.
        baseRevisionId:
          type: [string, 'null']
          format: uuid
          description: >
            UUID of the revision this issuance is compared against for
            addendum generation (ADR-066), or null when no base was recorded.

    RevisionSpecEntry:
      type: object
      required: [specId, position, tree]
      properties:
        specId:
          type: string
          format: uuid
        position:
          type: integer
          description: 1-based membership order at issuance
        tree:
          $ref: '#/components/schemas/SpecTree'

    RevisionWithTrees:
      type: object
      required:
        [
          revisionId,
          packageId,
          label,
          displayName,
          type,
          date,
          sortOrder,
          number,
          attributes,
          issuedAt,
          specs,
          parentRevisionId,
          baseRevisionId,
        ]
      properties:
        revisionId:
          type: string
          format: uuid
        packageId:
          type: string
          format: uuid
        label:
          type: string
        displayName:
          type: string
        type:
          type: string
        date:
          type: string
          format: date
        sortOrder:
          type: integer
          minimum: 1
        number:
          type: [string, 'null']
        attributes:
          $ref: '#/components/schemas/RevisionAttributes'
        issuedAt:
          type: string
          format: date-time
        specs:
          type: array
          items:
            $ref: '#/components/schemas/RevisionSpecEntry'
        parentRevisionId:
          type: [string, 'null']
          format: uuid
          description: >
            UUID of the revision this one was issued from (ADR-066 custody),
            or null for a root revision.
        baseRevisionId:
          type: [string, 'null']
          format: uuid
          description: >
            UUID of the revision this issuance is compared against for
            addendum generation (ADR-066), or null when no base was recorded.

    # ── Templates ──────────────────────────────────────────────────────────────

    StyleNodeType:
      type: string
      enum: [part, article, pr1, pr2, pr3, pr4, pr5, pr6, pr7]
      description: >
        CSI paragraph role. Maps to the nine generator levels: `part` (PART 1 –
        GENERAL), `article` (1.1 REFERENCES), `pr1`–`pr7` (A. / 1. / a. / 1) /
        a), then repeated 1) / a) paragraph tiers).

    StyleRule:
      type: object
      required: [nodeType, properties]
      properties:
        nodeType:
          $ref: '#/components/schemas/StyleNodeType'
        properties:
          $ref: '#/components/schemas/StyleProperties'

    StyleProperties:
      type: object
      description: >
        OOXML-derived style properties for a CSI paragraph role. Recognized keys
        are documented below; **unknown OOXML keys are preserved** via the JSONB
        catchall schema. All sub-objects also accept additional OOXML properties.
      properties:
        rPr:
          type: object
          description: Run (character) properties
          properties:
            rFonts:
              type: object
              description: Font references
              properties:
                ascii:
                  type: string
                hAnsi:
                  type: string
                cs:
                  type: string
                eastAsia:
                  type: string
              additionalProperties: true
            sz:
              type: integer
              description: Font size in half-points (e.g. 20 = 10 pt)
            b:
              type: boolean
              description: Bold
            i:
              type: boolean
              description: Italic
            caps:
              type: boolean
              description: All-caps
            smallCaps:
              type: boolean
            u:
              type: string
              description: Underline type (e.g. "single")
            strike:
              type: boolean
            color:
              type: string
              description: 'RRGGBB hex or "auto" (e.g. "FF0000")'
            highlight:
              type: string
          additionalProperties: true
        pPr:
          type: object
          description: Paragraph properties
          properties:
            spacing:
              type: object
              properties:
                before:
                  type: integer
                  description: Space before in twips
                after:
                  type: integer
                  description: Space after in twips
                line:
                  type: integer
                  description: Line spacing value in twips
                lineRule:
                  type: string
                  enum: [auto, exact, atLeast]
                contextualSpacing:
                  type: boolean
              additionalProperties: true
            ind:
              type: object
              description: Indentation (twips)
              properties:
                left:
                  type: integer
                right:
                  type: integer
                firstLine:
                  type: integer
                hanging:
                  type: integer
              additionalProperties: true
            jc:
              type: string
              enum: [left, center, right, both, distribute, start, end]
          additionalProperties: true
        numbering:
          type: object
          description: Numbering definition (from `numbering.xml` level)
          properties:
            ilvl:
              type: integer
            numFmt:
              type: string
            lvlText:
              type: string
            start:
              type: integer
          additionalProperties: true
      additionalProperties: true

    TemplateMeta:
      type: object
      required: [id, name, owner, libraryId, createdAt]
      description: Template identity and metadata without style rules
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Unique template name
        owner:
          type: [string, 'null']
          description: Firm / owner label, or null if not provided
        libraryId:
          type: [string, 'null']
          format: uuid
          description: >
            Owning library (#318). `null` = a built-in / global template (the
            seeded default, plus templates that predate library scoping).
            Onboarded templates are created with the spec's own `library_id`,
            not `null`. A library-scoped template can only be assigned to a spec
            in that same library.
        createdAt:
          type: string
          format: date-time

    Template:
      allOf:
        - $ref: '#/components/schemas/TemplateMeta'
        - type: object
          required: [rules]
          properties:
            rules:
              type: array
              items:
                $ref: '#/components/schemas/StyleRule'
              description: One rule per styleable NodeType (9 max)

    DerivationReport:
      type: object
      required: [nodeTypes, skippedNodeTypes, vanishSkipped]
      description: >
        Per-NodeType derivation audit trail returned alongside the template. Use
        this to review confidence levels, intent disagreements, and rejected
        outlier values before accepting the template as authoritative.
      properties:
        nodeTypes:
          type: array
          items:
            $ref: '#/components/schemas/NodeTypeReport'
          description: Report for each NodeType that had at least one paragraph
        skippedNodeTypes:
          type: array
          items:
            $ref: '#/components/schemas/StyleNodeType'
          description: NodeTypes with zero paragraphs in the source document
        vanishSkipped:
          type: integer
          description: >
            Count of `w:vanish` (hidden) paragraphs excluded from derivation.
            Hidden paragraphs are editorial instructions, not layout specimens.

    NodeTypeReport:
      type: object
      required: [nodeType, paragraphCount, styledCount, modalStyleId, decisions]
      properties:
        nodeType:
          $ref: '#/components/schemas/StyleNodeType'
        paragraphCount:
          type: integer
          description: Total non-vanish paragraphs for this NodeType
        styledCount:
          type: integer
          description: Paragraphs that carried a recognised Word style ID
        modalStyleId:
          type: [string, 'null']
          description: >
            Most-common Word style ID among voters. Used as the **intent**
            reference in §5 consensus derivation: when no majority exists,
            the modal style's own property value is preferred over the median.
        decisions:
          type: array
          items:
            $ref: '#/components/schemas/PropertyDecision'
          description: One decision per derived OOXML property path

    PropertyDecision:
      type: object
      required: [path, value, source, confidence, disagreesWithIntent, rejected]
      description: >
        Decision record for a single OOXML property (e.g. `rPr.sz`, `pPr.ind.left`).
      properties:
        path:
          type: string
          description: Dotted OOXML leaf path (e.g. "rPr.b", "pPr.ind.left")
        value:
          description: Chosen property value
        source:
          type: string
          enum: [consensus, intent, median, single, mode]
          description: >
            Algorithm branch that produced this value.
            `consensus` — strict majority (>50%) agreed.
            `intent` — no majority; modal style's own value used as tie-breaker.
            `median` — no majority, no intent, numeric values; lower-middle median chosen.
            `single` — only one styled paragraph for this NodeType.
            `mode` — low-plurality non-numeric fallback (first-seen tie-break).
        confidence:
          type: number
          minimum: 0
          maximum: 1
          description: Fraction of styled voters that agreed on the chosen value
        disagreesWithIntent:
          type: boolean
          description: >
            True when `source` is not `intent` but the modal style's value differs
            from the chosen value. Flag for human review.
        rejected:
          type: array
          description: Alternate values that were not chosen
          items:
            type: object
            required: [value, count]
            properties:
              value:
                description: The rejected property value
              count:
                type: integer
                description: Number of voters that carried this value

    # ── Editability classification (ADR-022) ────────────────────────────────────

    SpecNodeEditability:
      type: object
      required: [value, confidence, evidence]
      properties:
        value:
          type: string
          enum: [locked, editable, choice, note]
        confidence:
          type: number
          minimum: 0
          maximum: 1
        evidence:
          type: array
          items:
            type: object
            additionalProperties: true
        override:
          type: string
          enum: [locked, editable, choice, note]

    ReclassifyReport:
      type: object
      required: [specId, persisted, total, changed, entries]
      properties:
        specId:
          type: string
          format: uuid
        persisted:
          type: boolean
          description: false on a preview pass (diff computed, nothing written).
        total:
          type: integer
          minimum: 0
        changed:
          type: integer
          minimum: 0
          description: Entries whose machine value changed from before to after.
        entries:
          type: array
          items:
            $ref: '#/components/schemas/EditabilityDiffEntry'

    EditabilityDiffEntry:
      type: object
      required: [nodeId, before, after, overrideDisagrees]
      properties:
        nodeId:
          type: string
          format: uuid
        before:
          oneOf:
            - type: string
              enum: [locked, editable, choice, note]
            - type: 'null'
          description: Machine classification before this pass (null = was unclassified).
        after:
          type: string
          enum: [locked, editable, choice, note]
        overrideDisagrees:
          type: boolean
          description: A standing human override now differs from the fresh machine verdict.

    # ── Editing conventions (ADR-022 O-10) ──────────────────────────────────────

    ConventionRules:
      type: object
      description: >
        Open editing-convention ruleset. The named keys below are recognised;
        unknown keys are preserved verbatim (round-trip). `noteBanners` are
        user-supplied regex SOURCES bounded for safety at the write boundary.
      additionalProperties: true
      properties:
        colorMeanings:
          type: array
          items:
            type: object
            additionalProperties: true
        choiceTokens:
          type: array
          items:
            type: object
            additionalProperties: true
        noteBanners:
          type: array
          items:
            type: string
          description: Regex sources matching note/specifier banners
        comments:
          type: object
          additionalProperties: true
        defaultEditability:
          type: string
          enum: [locked, editable, choice, note]

    Library:
      type: object
      required: [id, tier, name, owner, parentLibraryId, createdAt]
      properties:
        id:
          type: string
          format: uuid
        tier:
          type: string
          enum: [reference, company, client]
        name:
          type: string
        owner:
          type: [string, 'null']
        parentLibraryId:
          type: [string, 'null']
          format: uuid
          description: Parent library (e.g. a client master's company parent), or null
        createdAt:
          type: string
          format: date-time

    LibrarySpec:
      type: object
      required: [specId, section, title, nodeCount, withdrawnAt, discipline]
      properties:
        specId:
          type: string
          format: uuid
        section:
          type: string
        title:
          type: string
        nodeCount:
          type: integer
          description: Number of paragraph nodes in the spec's tree
        withdrawnAt:
          type: [string, 'null']
          format: date-time
          description: >
            Soft-delete (withdraw) tombstone (ADR-030): ISO-8601 when the master
            is withdrawn, `null` when active. Withdrawn rows appear only when the
            request passes `includeWithdrawn=true`.
        discipline:
          type: [string, 'null']
          description: >
            Resolved discipline key under the library's mapping (ADR-065), or
            `null` when the spec's CSI division is unmapped.

    ProjectSpec:
      type: object
      required: [specId, section, title, position, discipline]
      properties:
        specId:
          type: string
          format: uuid
        section:
          type: string
        title:
          type: string
        position:
          type: integer
          description: 1-based TOC position within the project
        discipline:
          type: [string, 'null']
          description: >
            Resolved discipline key under the built-in default mapping (ADR-065),
            or `null` when the spec's CSI division is unmapped.

    DisciplineRuleView:
      type: object
      required: [divisionStart, divisionEnd]
      description: An inclusive CSI division range (2-digit) mapped to a discipline.
      properties:
        divisionStart:
          type: string
          pattern: '^\d{2}$'
        divisionEnd:
          type: string
          pattern: '^\d{2}$'

    ResolvedDiscipline:
      type: object
      required: [id, key, name, rules]
      properties:
        id:
          type: string
          format: uuid
        key:
          type: string
          description: Stable slug used as the filter value on spec listings
        name:
          type: string
          description: Display label
        rules:
          type: array
          description: Division ranges mapped to this discipline under the resolved scope (may be empty)
          items:
            $ref: '#/components/schemas/DisciplineRuleView'

    DisciplineRuleInput:
      type: object
      required: [discipline, divisionStart, divisionEnd]
      properties:
        discipline:
          type: string
          minLength: 1
          description: Discipline key from GET /disciplines
        divisionStart:
          type: string
          pattern: '^\d{2}$'
        divisionEnd:
          type: string
          pattern: '^\d{2}$'
          description: Must be >= divisionStart

    SetDisciplinesBody:
      type: object
      required: [rules]
      properties:
        rules:
          type: array
          minItems: 1
          description: Complete replacement rule set; division ranges must not overlap
          items:
            $ref: '#/components/schemas/DisciplineRuleInput'

    EditingConvention:
      type: object
      required: [id, libraryId, name, rules, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        libraryId:
          type: [string, 'null']
          format: uuid
          description: Owning library, or null for a built-in industry default
        name:
          type: string
        rules:
          $ref: '#/components/schemas/ConventionRules'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PutConventionBody:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
        rules:
          $ref: '#/components/schemas/ConventionRules'

    CloneConventionBody:
      type: object
      required: [sourceId]
      properties:
        sourceId:
          type: string
          format: uuid
          description: A built-in or another library's convention profile id

    # ── Revision nomenclature profiles (ADR-025 / #209) ───────────────────────

    RevisionNomenclatureType:
      type: object
      required: [key]
      additionalProperties: true
      properties:
        key:
          type: string
          minLength: 1
        name:
          type: string
        format:
          type: object
          additionalProperties: true
          properties:
            displayName:
              type: string
              description: Template using `{attributeKey}` placeholders
            number:
              type: string
              description: Template for the rendered revision number
        fields:
          type: array
          items:
            type: object
            required: [key]
            additionalProperties: true
            properties:
              key:
                type: string
                minLength: 1
              kind:
                type: string
                enum: [string, integer, number, date, boolean, json]
              required:
                type: boolean
              sequence:
                type: string

    RevisionNomenclatureProfile:
      type: object
      required: [id, projectId, name, types, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        projectId:
          type: [string, 'null']
          format: uuid
          description: Owning project, or null for a built-in default
        name:
          type: string
        types:
          type: array
          items:
            $ref: '#/components/schemas/RevisionNomenclatureType'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PutRevisionNomenclatureBody:
      type: object
      required: [name, types]
      properties:
        name:
          type: string
          minLength: 1
        types:
          type: array
          items:
            $ref: '#/components/schemas/RevisionNomenclatureType'

    CloneRevisionNomenclatureBody:
      type: object
      required: [sourceId]
      properties:
        sourceId:
          type: string
          format: uuid
          description: A built-in or another project's revision nomenclature profile id

    ParagraphAssociation:
      type: object
      required: [id, label, externalMetadata, createdAt]
      description: External content link associated with a paragraph node (#109).
      properties:
        id:
          type: string
          format: uuid
        label:
          type: string
          minLength: 1
        externalProvider:
          type: string
          description: DMS connector identity (ADR-014 D5). Present together with externalId or absent.
        externalId:
          type: string
          description: Document identifier within the DMS. Present together with externalProvider or absent.
        url:
          type: string
          format: uri
          description: Direct URL provenance for firms without a DMS connector.
        contentHash:
          type: string
          description: sha256 hex digest of the referenced bytes, when known.
        externalMetadata:
          type: object
          additionalProperties: true
          description: Arbitrary provider-specific metadata (empty object when none supplied).
        createdAt:
          type: string
          format: date-time

    CreateAssociation:
      type: object
      required: [label]
      description: >
        Request body for associating external content with a paragraph.
        Either a url, or both externalProvider and externalId, must be provided.
        externalProvider and externalId are both-or-neither — a lone one of the
        pair is rejected (422) even when a url is present.
      dependentRequired:
        externalProvider: [externalId]
        externalId: [externalProvider]
      properties:
        label:
          type: string
          minLength: 1
        externalProvider:
          type: string
          description: DMS connector identity. Always paired with externalId (both-or-neither).
        externalId:
          type: string
          description: Document identifier within the DMS. Always paired with externalProvider (both-or-neither).
        url:
          type: string
          format: uri
          description: Direct URL. Required when externalProvider/externalId are absent.
        contentHash:
          type: string
          description: sha256 hex digest of the referenced bytes.
        externalMetadata:
          type: object
          additionalProperties: true
          description: Arbitrary provider-specific metadata.

    NumberingProfile:
      type: object
      description: >
        Extracted numbering configuration from a DOCX file, and the WRITE
        contract for POST/PATCH /numbering-profiles. Carries tiers (CSI
        hierarchy), numbering (numId→levels map), styleLadder (style→tier
        map), and articleIlvl (the ilvl at which the Article tier sits, always
        >= 1 — ilvl 0 is the PART tier). Open schema — unknown keys are
        preserved for forward-compatibility.


        `tier` on each numbering[].levels[] and styleLadder[] entry is a
        DERIVED annotation, not authoritative input (ADR-067): classification
        is driven by ilvl + articleIlvl alone. Omit `tier` and the server
        fills it from tierForIlvl(ilvl, articleIlvl); declare one that
        disagrees with that derivation and the write is rejected (422,
        entry-naming message). articleIlvl becomes required as soon as
        numbering or styleLadder is non-empty, since the derivation is
        undecidable without it.
      additionalProperties: true
      required: [tiers, numbering, styleLadder]
      properties:
        tiers:
          type: object
          additionalProperties: true
          required: [part]
          description: >
            CSI tier shape. `part` is required and pinned to the CSI integer model
            (integer style, 1 to 5 parts); article/paragraph/subparagraph are
            optional. Unknown keys are preserved (open schema).
          properties:
            part:
              type: object
              additionalProperties: true
              required: [numberStyle, maxCount]
              properties:
                numberStyle:
                  type: string
                  enum: [integer]
                  description: CSI parts are integer-numbered.
                maxCount:
                  type: integer
                  minimum: 1
                  maximum: 5
                  description: Maximum PART count (CSI bound, at most 5).
            article:
              type: object
              additionalProperties: true
              properties:
                maxCount:
                  type: integer
                  minimum: 1
            paragraph:
              type: object
              additionalProperties: true
              properties:
                maxCount:
                  type: integer
                  minimum: 1
            subparagraph:
              type: object
              additionalProperties: true
              properties:
                maxCount:
                  type: integer
                  minimum: 1
        numbering:
          type: array
          description: Per-numId level definitions.
          items:
            type: object
            additionalProperties: true
            required: [numId, levels]
            properties:
              numId:
                type: integer
              levels:
                type: array
                items:
                  type: object
                  additionalProperties: true
                  required: [ilvl]
                  properties:
                    ilvl:
                      type: integer
                      minimum: 0
                    tier:
                      type: string
                      enum: [part, article, paragraph, subparagraph]
                      description: >
                        Optional — derived from ilvl + articleIlvl when
                        omitted (ADR-067); a declared value that disagrees
                        with the derivation is rejected (422).
                    labelTemplate:
                      type: string
                    numFmt:
                      type: string
        styleLadder:
          type: array
          description: Per-styleId tier assignment derived from the style cascade.
          items:
            type: object
            additionalProperties: true
            required: [styleId, numId, ilvl]
            properties:
              styleId:
                type: string
              numId:
                type: integer
              ilvl:
                type: integer
                minimum: 0
              tier:
                type: string
                enum: [part, article, paragraph, subparagraph]
                description: >
                  Optional — derived from ilvl + articleIlvl when omitted
                  (ADR-067); a declared value that disagrees with the
                  derivation is rejected (422).
        articleIlvl:
          type: integer
          minimum: 1
          description: >
            Ilvl of the Article tier. Always >= 1 — ilvl 0 is reserved for the
            PART tier, so the Article level can never be 0. Required as soon
            as numbering or styleLadder is non-empty — the server needs it to
            derive (and validate) each entry's tier (ADR-067); a body that
            supplies rules without it is rejected (422).

    NumberingProfileRead:
      type: object
      description: >
        Lenient READ contract for a persisted numbering profile (ADR-061, #323).
        Same shape as `NumberingProfile` — required fields, JS types, the closed
        `tier` vocabulary, the `numberStyle: integer` literal, open-schema
        passthrough, AND the structural floor of every numeric field (`ilvl >= 0`,
        counts `>= 1`) are all still enforced — but the numeric POLICY bounds a
        write-side tightening could newly impose are relaxed to that structural
        floor: `part.maxCount` drops the CSI ceiling of 5, and `articleIlvl`
        relaxes from `>= 1` to `>= 0` (the legacy #320 case). It is NOT a rubber
        stamp — a sub-structural value like a negative `articleIlvl` is corruption
        (it would misparse downstream) and still fails. Reads re-validate stored
        JSONB, so a row persisted under an older, looser write contract reads back
        cleanly instead of 500ing once the write schema tightens; writes stay
        strict at ingress against `NumberingProfile`. This is the response shape
        for numbering-profile reads (`NumberingProfileRow.rules`).
      additionalProperties: true
      required: [tiers, numbering, styleLadder]
      properties:
        tiers:
          type: object
          additionalProperties: true
          required: [part]
          description: >
            CSI tier shape. `part` is required and pinned to the CSI integer model
            (integer style); article/paragraph/subparagraph are optional. Numeric
            bounds are relaxed on read (see schema description). Unknown keys are
            preserved (open schema).
          properties:
            part:
              type: object
              additionalProperties: true
              required: [numberStyle, maxCount]
              properties:
                numberStyle:
                  type: string
                  enum: [integer]
                  description: CSI parts are integer-numbered.
                maxCount:
                  type: integer
                  minimum: 1
                  description: >
                    Maximum PART count. Read-lenient — the CSI policy ceiling of 5 is
                    write-only, but the structural floor (>= 1) is still enforced.
            article:
              type: object
              additionalProperties: true
              properties:
                maxCount:
                  type: integer
                  minimum: 1
            paragraph:
              type: object
              additionalProperties: true
              properties:
                maxCount:
                  type: integer
                  minimum: 1
            subparagraph:
              type: object
              additionalProperties: true
              properties:
                maxCount:
                  type: integer
                  minimum: 1
        numbering:
          type: array
          description: Per-numId level definitions.
          items:
            type: object
            additionalProperties: true
            required: [numId, levels]
            properties:
              numId:
                type: integer
              levels:
                type: array
                items:
                  type: object
                  additionalProperties: true
                  required: [ilvl, tier]
                  properties:
                    ilvl:
                      type: integer
                      minimum: 0
                    tier:
                      type: string
                      enum: [part, article, paragraph, subparagraph]
                    labelTemplate:
                      type: string
                    numFmt:
                      type: string
        styleLadder:
          type: array
          description: Per-styleId tier assignment derived from the style cascade.
          items:
            type: object
            additionalProperties: true
            required: [styleId, numId, ilvl, tier]
            properties:
              styleId:
                type: string
              numId:
                type: integer
              ilvl:
                type: integer
                minimum: 0
              tier:
                type: string
                enum: [part, article, paragraph, subparagraph]
        articleIlvl:
          type: integer
          minimum: 0
          description: >
            Ilvl of the Article tier. Read-lenient — the write-side policy bound is
            relaxed from `>= 1` to its structural floor `>= 0`, so a legacy
            `articleIlvl: 0` row reads back cleanly while a negative (corrupt) value
            still fails.

    NumberingProfileRow:
      type: object
      description: A persisted numbering profile row from the database.
      required: [id, libraryId, name, rules, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        libraryId:
          type: [string, 'null']
          format: uuid
          description: Owning library UUID, or null for the built-in CSI Default.
        name:
          type: string
        rules:
          # Read-lenient (ADR-061, #323): reads re-validate stored JSONB, so this
          # response can carry a legacy row whose values fall outside the strict
          # write bounds. Write request bodies still ref the strict NumberingProfile.
          $ref: '#/components/schemas/NumberingProfileRead'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    # ─── Standards registry (#446, ADR-064) ─────────────────────────────────────
    StandardStatus:
      type: string
      enum: [current, superseded, withdrawn, unknown]
      description: Registry currency verdict; 'unknown' until a client records one.
    StandardCitingSpec:
      type: object
      required: [specId, section]
      properties:
        specId: { type: string, format: uuid }
        section: { type: string }
    StandardsRollup:
      type: object
      description: >
        One-call standards rollup for a project or library (#446). Compiles the
        distinct standards cited in scope, each joined to its global registry
        verdict, plus superseded/withdrawn findings and summary counts.
      required: [scope, standards, findings, summary, anchorCap, notes]
      properties:
        scope:
          type: object
          required: [type, id]
          properties:
            type: { type: string, enum: [project, library] }
            id: { type: string, format: uuid }
        standards:
          type: array
          items: { $ref: '#/components/schemas/StandardRollupRow' }
        findings:
          type: array
          items: { $ref: '#/components/schemas/StandardFinding' }
        summary: { $ref: '#/components/schemas/StandardsSummary' }
        anchorCap:
          type: integer
          description: Max paragraph anchors emitted per standard.
        notes:
          type: array
          items: { type: string }
    StandardRollupRow:
      type: object
      required:
        [
          orgCode,
          standardCode,
          citationCount,
          citingSpecs,
          anchors,
          anchorsTruncated,
          registered,
          status,
          currentVersion,
          sourceUrl,
          title,
          lastVerifiedAt,
          notes,
        ]
      properties:
        orgCode: { type: string }
        standardCode: { type: string }
        citationCount: { type: integer }
        citingSpecs:
          type: array
          items: { $ref: '#/components/schemas/StandardCitingSpec' }
        anchors:
          type: array
          description: Paragraph UUIDs citing the standard, capped at anchorCap.
          items: { type: string, format: uuid }
        anchorsTruncated:
          type: boolean
          description: True when citationCount exceeded anchorCap.
        registered:
          type: boolean
          description: True when a registry verdict exists for this standard.
        status: { $ref: '#/components/schemas/StandardStatus' }
        currentVersion: { type: [string, 'null'] }
        sourceUrl: { type: [string, 'null'] }
        title: { type: [string, 'null'] }
        lastVerifiedAt: { type: [string, 'null'], format: date-time }
        notes: { type: [string, 'null'] }
    StandardFinding:
      type: object
      description: A cited standard the registry marks superseded or withdrawn.
      required: [type, orgCode, standardCode, status, citingSpecs, anchors]
      properties:
        type: { type: string, enum: [standard_superseded, standard_withdrawn] }
        orgCode: { type: string }
        standardCode: { type: string }
        status: { $ref: '#/components/schemas/StandardStatus' }
        citingSpecs:
          type: array
          items: { $ref: '#/components/schemas/StandardCitingSpec' }
        anchors:
          type: array
          items: { type: string, format: uuid }
    StandardsSummary:
      type: object
      required: [standards, registered, superseded, withdrawn, unverified, findings]
      properties:
        standards: { type: integer, description: Distinct cited standards. }
        registered: { type: integer, description: Cited standards with a registry verdict. }
        superseded: { type: integer }
        withdrawn: { type: integer }
        unverified: { type: integer, description: Cited standards with no last-verified timestamp. }
        findings: { type: integer }
    StandardRecord:
      type: object
      description: A registry record (the PUT verdict response).
      required:
        [
          id,
          orgCode,
          standardCode,
          title,
          currentVersion,
          sourceUrl,
          status,
          lastVerifiedAt,
          notes,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string, format: uuid }
        orgCode: { type: string }
        standardCode: { type: string }
        title: { type: [string, 'null'] }
        currentVersion: { type: [string, 'null'] }
        sourceUrl: { type: [string, 'null'] }
        status: { $ref: '#/components/schemas/StandardStatus' }
        lastVerifiedAt: { type: [string, 'null'], format: date-time }
        notes: { type: [string, 'null'] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StandardVerificationBody:
      type: object
      description: >
        A standards verdict (PUT-replace, ADR-064 §3). Omitted optional fields
        reset to null; status defaults to 'unknown'. The server stamps
        last_verified_at on every write.
      properties:
        status: { $ref: '#/components/schemas/StandardStatus' }
        currentVersion: { type: [string, 'null'], minLength: 1, maxLength: 200 }
        sourceUrl: { type: [string, 'null'], format: uri, maxLength: 2000 }
        title: { type: [string, 'null'], minLength: 1, maxLength: 500 }
        notes: { type: [string, 'null'], maxLength: 5000 }

    # ── Header/footer config (#476, ADR-040) ─────────────────────────────────────

    HeaderFooterScope:
      description: >
        Which scope a header/footer config row is anchored to — exactly one
        of a client library, a project, a design package, or an issued
        revision.
      oneOf:
        - type: object
          required: [kind, clientLibraryId]
          properties:
            kind: { type: string, enum: [client] }
            clientLibraryId: { type: string, format: uuid }
        - type: object
          required: [kind, projectId]
          properties:
            kind: { type: string, enum: [project] }
            projectId: { type: string, format: uuid }
        - type: object
          required: [kind, packageId]
          properties:
            kind: { type: string, enum: [package] }
            packageId: { type: string, format: uuid }
        - type: object
          required: [kind, revisionId]
          properties:
            kind: { type: string, enum: [revision] }
            revisionId: { type: string, format: uuid }
      discriminator:
        propertyName: kind

    HeaderFooterComposition:
      type: object
      description: >
        JSONB-backed header/footer composition (ADR-040). v1 (#208) top-level
        `header`/`footer`/`style` describe a single page variant and are read
        as the `default` variant unless an explicit `variants.default` is
        also present, in which case `variants.default` wins. v2 (#302) adds
        `variants.first`/`variants.even` (Word title-page and even/odd
        headers), `pageNumbering`, and an open `raw` sidecar for
        captured-but-unmodeled OOXML markup plus parser warnings (#306).
        Every nested object is open (`additionalProperties: true`, ADR-021)
        — unknown JSON-safe keys, including at the top level, are preserved
        verbatim on write and round-tripped on read.
      additionalProperties: true
      properties:
        header:
          type: object
          additionalProperties: true
          description: A header region (left/center/right cells, region style, rule line).
          properties:
            left: &headerFooterCell
              type: object
              additionalProperties: true
              properties:
                content:
                  type: array
                  items: &headerFooterField
                    type: object
                    additionalProperties: true
                    required: [kind]
                    properties:
                      kind:
                        type: string
                        enum:
                          [
                            date,
                            sectionTitle,
                            sectionNumber,
                            pageNumber,
                            packageName,
                            revisionName,
                            revisionLabel,
                            projectName,
                            projectNumber,
                            clientName,
                            clientNumber,
                            literal,
                            image,
                          ]
                      source: { type: string, enum: [issuance, current] }
                      text: { type: string }
                      label: { type: string }
                      format: { type: string }
                      prefix: { type: string }
                      suffix: { type: string }
                      imageData:
                        type: string
                        maxLength: 6990508
                        description: >
                          Base64-encoded image bytes for a `kind: "image"`
                          field (#308, ADR-069). The generator never trusts
                          `imageMediaType` — it re-sniffs the actual bytes'
                          magic-byte signature and renders using that result,
                          so a stale/unsupported `imageMediaType` still
                          round-trips here; it just will not render. The
                          `maxLength` is the base64-string cap corresponding
                          to the 5 MB decoded-byte limit
                          (`MAX_IMAGE_BASE64_LENGTH`,
                          `src/lib/image-media-type.ts`) enforced by the
                          paired Zod schema (`src/ast/header-footer-schemas.ts`).
                      imageMediaType: { type: string }
                      widthEmu: { type: integer, minimum: 1 }
                      heightEmu: { type: integer, minimum: 1 }
                      altText: { type: string }
                separator: { type: string }
                style: &headerFooterVisualStyle
                  type: object
                  additionalProperties: true
                  properties:
                    fontFamily: { type: string }
                    fontSizeHalfPt: { type: integer }
                    bold: { type: boolean }
                    italic: { type: boolean }
                    caps: { type: boolean }
                    color: { type: string }
            center: *headerFooterCell
            right: *headerFooterCell
            style: *headerFooterVisualStyle
            ruleLine: &headerFooterRuleLine
              type: object
              additionalProperties: true
              properties:
                enabled: { type: boolean }
                widthTwips: { type: integer }
                color: { type: string }
                style: { type: string }
            table: &headerFooterTable
              type: object
              additionalProperties: true
              description: >
                A table/grid layout for header/footer content (#309,
                ADR-071) — a sibling slot to `left`/`center`/`right`, not a
                replacement; a region may render both a paragraph and a
                table, paragraph first. Cell content reuses the same field
                schema as `left`/`center`/`right`'s `content`; images are
                structurally excluded from table cells at render time, not
                by this schema (ADR-071 decision 3).
              required: [rows]
              properties:
                rows:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    additionalProperties: true
                    required: [cells]
                    properties:
                      cells:
                        type: array
                        items:
                          type: object
                          additionalProperties: true
                          properties:
                            content:
                              type: array
                              items: *headerFooterField
                            columnSpan: { type: integer, minimum: 1 }
                            separator: { type: string }
                            style: *headerFooterVisualStyle
                columnWidths:
                  type: array
                  items: { type: integer, minimum: 1 }
                borders: *headerFooterRuleLine
        footer:
          type: object
          additionalProperties: true
          description: A footer region — same shape as `header`.
          properties:
            left: *headerFooterCell
            center: *headerFooterCell
            right: *headerFooterCell
            style: *headerFooterVisualStyle
            ruleLine: *headerFooterRuleLine
            table: *headerFooterTable
        style: *headerFooterVisualStyle
        variants:
          type: object
          additionalProperties: true
          description: >
            Word-style page variants (ADR-040): `default` applies to every
            page unless a more specific variant overrides it; `first`
            overrides the title page (`w:titlePg`); `even` overrides even
            pages (`w:evenAndOddHeaders`).
          properties:
            default: &headerFooterVariant
              type: object
              additionalProperties: true
              properties:
                header:
                  type: object
                  additionalProperties: true
                  properties:
                    left: *headerFooterCell
                    center: *headerFooterCell
                    right: *headerFooterCell
                    style: *headerFooterVisualStyle
                    ruleLine: *headerFooterRuleLine
                    table: *headerFooterTable
                footer:
                  type: object
                  additionalProperties: true
                  properties:
                    left: *headerFooterCell
                    center: *headerFooterCell
                    right: *headerFooterCell
                    style: *headerFooterVisualStyle
                    ruleLine: *headerFooterRuleLine
                    table: *headerFooterTable
                style: *headerFooterVisualStyle
            first: *headerFooterVariant
            even: *headerFooterVariant
        pageNumbering:
          type: object
          additionalProperties: true
          required: [mode]
          description: >
            Page-numbering policy: continuous across the package, or
            restarting at each spec section. `startAt` seeds the first
            rendered number (`w:pgNumType@start`).
          properties:
            mode: { type: string, enum: [continuous, restartPerSpec] }
            startAt: { type: integer }
        raw:
          type: object
          additionalProperties: true
          description: >
            Open sidecar for captured-but-unmodeled header/footer OOXML plus
            parser warnings (#306) — never lost on round-trip.
          properties:
            warnings:
              type: array
              items: { type: string }

    HeaderFooterConfig:
      type: object
      description: A stored header/footer config row for exactly one scope.
      required: [id, scope, config, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        scope: { $ref: '#/components/schemas/HeaderFooterScope' }
        config: { $ref: '#/components/schemas/HeaderFooterComposition' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    HeaderFooterResolutionContext:
      type: object
      description: >
        The anchor chain resolved for a project/package/revision read —
        which client library (if any) feeds the top of the precedence chain,
        and how far down the package/revision chain the anchor reaches.
        `packageId`/`revisionId` are present only when the anchor is at or
        below that level.
      required: [clientLibraryId, projectId]
      properties:
        clientLibraryId: { type: [string, 'null'], format: uuid }
        projectId: { type: string, format: uuid }
        packageId: { type: string, format: uuid }
        revisionId: { type: string, format: uuid }

    ResolvedHeaderFooterConfig:
      type: object
      description: >
        The effective header/footer config for a project/package/revision
        anchor, merged client -> project -> package -> revision in that
        precedence order (later layers win per-key, ADR-040). `layers` lists
        every contributing config row low-to-high precedence — the winning
        scope for any given key is `layers[layers.length - 1].scope`,
        whichever layer last touched it; no separate winning-scope field is
        synthesized.
      required: [context, layers, config]
      properties:
        context: { $ref: '#/components/schemas/HeaderFooterResolutionContext' }
        layers:
          type: array
          items: { $ref: '#/components/schemas/HeaderFooterConfig' }
        config: { $ref: '#/components/schemas/HeaderFooterComposition' }
