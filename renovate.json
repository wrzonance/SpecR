{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "minimumReleaseAge": "7 days",
  "automerge": false,
  "labels": ["dependencies"],
  "commitMessagePrefix": "chore(deps):",
  "packageRules": [
    {
      "matchManagers": ["npm"],
      "groupName": "npm-deps"
    },
    {
      "description": "Major bumps for OOXML/DOCX libs require manual review — may change numbering or content control behavior",
      "matchPackageNames": ["docx", "jszip"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Node pin (ADR-081): only propose npm releases whose own engines.node overlaps ours. The constraint itself is auto-detected from engines.node and .nvmrc — deliberately NOT declared in a `constraints` block, because Renovate treats a manually-set constraint as fixed and will never offer to bump it, which would fork the source of truth. Scoped to runtime dependencies per Renovate's guidance that strict filtering across devDependencies filters far more than most users expect.",
      "matchManagers": ["npm"],
      "matchDepTypes": ["dependencies"],
      "constraintsFiltering": "strict"
    },
    {
      "description": "Node pin (ADR-081): never propose a Node MAJOR bump. Moving LTS majors is a deliberate coordinated change across .nvmrc, both engines.node ranges, both @types/node majors and CI — not a routine dependency PR. Minor/patch within the pinned major stays enabled.",
      "matchDepNames": ["node"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Node pin (ADR-081): keep .nvmrc and any workflow node-version on the pinned major. Bounded rather than disabled outright so security patches inside 24.x still flow.",
      "matchDatasources": ["node-version"],
      "allowedVersions": "<25.0.0"
    },
    {
      "description": "Node pin (ADR-081): @types/node must track the runtime major. A typings-only bump past the runtime lets tsc accept APIs that do not exist at execution — exactly what PR #557 proposed (@types/node 26 against a Node 22 runtime) and why it was closed.",
      "matchPackageNames": ["@types/node"],
      "allowedVersions": "<25.0.0"
    },
    {
      "matchManagers": ["github-actions"],
      "labels": ["ci"],
      "commitMessagePrefix": "chore(ci):",
      "groupName": "github-actions"
    }
  ]
}
